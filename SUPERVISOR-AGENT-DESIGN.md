# 监督 Agent（Supervisor Agent）整体设计与实现

定位：本文定义"规则引擎执行 + Agent 监管"的双层运行时。规则引擎（现有的 run-flow-orchestrator、supervise-adaptive-export、各 skill 脚本）保持确定性执行；新增的监督 Agent 负责监管规则执行过程，在失败时代替人工完成检测、定位、修复、复盘、防复发五件事。

---

## 一、第一性原理分析

监督的本质是什么？把"系统在无人值守时遇到非预期状态"这个命题拆到最小：

公理 1（确定性二分）：任何执行步骤要么可被规则完全描述，要么不能。可被描述的交给规则引擎——规则便宜、可预测、可回归测试；不能被描述的（原因未知的失败、需要取舍的决策）才需要判断力。判断力昂贵且会出错，所以能用规则的绝不用 Agent。

公理 2（监督独立性）：监督者必须通过独立信道观察被监督者，且不共享失败路径。规则引擎报告自己失败的方式（状态收据、事件流）必须由 Agent 独立读取验证，而不是相信引擎自述的结论——引擎可能连"报告失败"这步都失败（例如卡死时根本不写收据）。

公理 3（四环闭环）：兜底能力 = 感知（检测）→ 诊断（定位）→ 处置（修复）→ 记忆（复盘）。缺任何一环都不是兜底：不能感知则永远不知道失败；不能诊断则修复是盲猜；不能处置则诊断是空谈；不能记忆则每次都是第一次。当前系统只有前三环的雏形（确定性卡停检测 + 监督者重启），第四环完全缺失——这正是"每次都从第一页重跑"式重复失败的结构性原因。

公理 4（行动边界）：Agent 的自主权必须被白名单、预算、幂等三重约束。白名单限定它能做的事的类型；预算限定它在时间窗口内能做多少次（防止修复本身成为故障源）；幂等保证同一处置重复执行不产生副作用。白名单之外的一切（涉及数据删除、对外发布、花钱、登录凭据）fail-closed 升级人工。

公理 5（经验可验证）：经验必须满足可机读、可验证、可过期。可机读——签名匹配是代码不是人读文档；可验证——每次经验应用的结果回写置信度，修复失败的经验自动降权直至清除（防止一次错误诊断被固化成永久错误策略）；可过期——经验绑定环境指纹，环境变了（如 Edge 版本升级）旧经验标记为待复核。

公理 6（修复必须验证）：没有验证的修复不算修复。每次处置后必须检查状态收据是否真的改善（status 从 STALLED 回到 RUNNING、completed_end 推进、导出产物落盘），验证通过才允许写入经验库。

## 二、职责分工边界

| 维度 | 规则引擎（执行层） | 监督 Agent（监管层） |
|---|---|---|
| 回答的问题 | "下一步执行什么" | "执行得对不对、失败了为什么" |
| 输入 | 状态收据（receipt） | 状态收据 + 事件流 + 故障上下文 + 经验库 |
| 输出 | 一个确定性的下一步动作 | 分类、诊断、处置决策、复盘记录 |
| 决策方式 | 状态机查表 | 经验匹配 → 规则诊断 → LLM 判断，三级降速 |
| 失败时行为 | 标记 HUMAN_REQUIRED 停止 | 接管：分类、定位、修复、验证、复盘 |
| 修改自身行为 | 不允许（只能被参数化） | 允许（通过经验库和参数建议） |
| 对人工的态度 | 一切非预期都是终点 | 一切非预期都是工作起点；仅白名单外升级人工 |

分界线：规则引擎永远不调用 LLM、不做启发式判断；监督 Agent 永远不直接执行业务流程（不采集、不导出、不发布），只通过白名单动作重新参数化或重启规则引擎。

## 三、五层架构与控制循环

```
┌──────────────────────────────────────────────────────┐
│ L5 记忆层 Experience Store                            │
│    经验条目（签名/根因/处方/置信度）+ 复盘报告归档      │
├──────────────────────────────────────────────────────┤
│ L4 处置层 Remediation Executor                        │
│    白名单动作 + 预算控制 + 幂等执行 + 结果验证          │
├──────────────────────────────────────────────────────┤
│ L3 诊断层 Triage Engine（三级降速）                    │
│    ① 经验库签名匹配（毫秒，零成本）                    │
│    ② 确定性规则诊断（本文件故障分类学）                 │
│    ③ LLM 诊断（最后手段，结构化输出，白名单校验）      │
├──────────────────────────────────────────────────────┤
│ L2 检测层 Anomaly Detector                            │
│    停滞检测、心跳超时、收据断更、产物缺失、日志异常模式  │
├──────────────────────────────────────────────────────┤
│ L1 感知层 Evidence Bus                                │
│    状态收据（DB）+ 事件流（events.jsonl）+ 进程/端口    │
└──────────────────────────────────────────────────────┘
```

控制循环（每次心跳执行一轮）：

```
watch（读收据与事件，计算活性指标）
  → detect（指标异常？无 → 等待下一心跳）
  → triage（经验匹配 → 规则诊断 → LLM 诊断）
  → decide（动作在白名单内？预算允许？）
  → act（执行动作，通常是"带新参数重启规则流程"）
  → verify（等待并读取新收据：改善 / 未改善 / 恶化）
  → record（验证通过 → 写经验 + 复盘；失败 → 降权该经验 + 升级人工）
```

## 四、故障处理四环节 → 组件映射

正常执行：监督 Agent 只做 watch + detect，对规则引擎零侵入（不注入、不代理、不改动其代码路径）。

失败检测：三个独立信号源交叉验证——① DB 收据 status 长期停在非 RUNNING 终态或超时未更新；② events.jsonl 最新事件时间距今超过活性阈值；③ 关键错误事件（STALL、PARTIAL_EXPORT_FAILED、HUMAN_REQUIRED）出现。任何两源同时异常即触发 triage（防误报）。

问题定位与修复：三级诊断。①经验库：用错误签名（failureClass + errorPattern + stage）做正则匹配，命中直接复用已验证处方，这是防复发的关键路径——同类故障第二次发生时不再经过任何判断，毫秒级出处置；②规则诊断：基于故障分类学的确定性判断（见 diagnose.mjs 的 FAILURE_CLASSES）；③LLM 诊断：构造包含故障上下文、事件尾部、历史经验的提示，要求输出结构化 JSON（failureClass/rootCause/remedy/actions），动作必须过白名单校验，任何字段非法即整体拒绝并升级人工。修复动作以"带修正参数重启规则流程"为主（最小侵入），辅以环境类动作（清端口、清锁、开浏览器）。

经验总结与防复发：每起验证通过的故障生成 postmortem（时间线 + 根因 + 处置 + 验证证据），并写入经验库；下次启动规则流程前，Agent 先用运行上下文（关键词、页数、插件版本、浏览器版本）匹配经验库做"预防性参数化"——例如已知的导出下载超时经验会在启动前就把导出等待时长调大，使同类故障根本没有机会发生。

## 五、实现清单

runtime/supervisor-agent/ 下：

- supervisor-agent.mjs —— 主循环与决策编排：handleIncident(incident, deps) 完成 triage → decide → act → verify → record；CLI 入口读 JSON 故障报告，--dry-run 只出决策不执行。
- diagnose.mjs —— 故障分类学与确定性诊断规则（STALL_PAGE_REQUEST / EXPORT_DOWNLOAD_TIMEOUT / LOGIN_REQUIRED / PLATFORM_CONTROL / BROWSER_DEBUG_PORT / UNKNOWN）。
- actions.mjs —— 白名单动作集 + Budget 预算控制 + 动作描述（restart_flow / retry_export / reopen_login / escalate_human / mark_experience_failed）。
- experience.mjs —— 经验库读写、签名匹配、置信度动力学（验证 +1，应用失败 -1，<=0 自动退役）。
- llm.mjs —— LLM 诊断钩子（OpenAI 兼容端点，环境变量配置，全链路 fail-closed，未配置返回 null 走升级人工）。
- experience.json —— 经验库种子：以 2026-09-12 夜间两个真实故障初始化。
- supervisor-agent.test.mjs —— 验收测试：以夜间真实故障为夹具，覆盖三级诊断、白名单拒绝、预算熔断、经验命中与降权。

安全边界：所有动作默认 dry-run；--apply 才执行；动作执行器通过依赖注入，测试中全部为桩；升级人工（escalate_human）是唯一"无边界"出口，因为它把决定权交还给人。

## 六、与现有系统的接入方式（零侵入）

监督 Agent 不改动任何现有脚本。接入点只有读和重启：

1. 读：PostgreSQL 收据表、runtime/*-runs/*/events.jsonl、xws-flow-result.json。
2. 重启：以新参数调用 run-flow-orchestrator.mjs（本来就支持参数化）。
3. 前置预防：新 run 启动前查询经验库，生成启动参数修正。

当前用 bash 脚本写的 xws-overnight-pipeline.sh 重试循环，正是这套监督逻辑的"人肉版"——本文将其规则化、经验化、可验证化。

## 七、验收标准（以真实故障为种子）

- 复现 09-12 故障 A（第 15 页请求挂起 + 部分导出 30 秒超时）：triage 输出 EXPORT_DOWNLOAD_TIMEOUT + STALL_PAGE_REQUEST 复合诊断，处方为"导出等待 120 秒 + 卡停阈值放宽"，动作 restart_flow_with_params 在白名单内。
- 复现故障 B（小旺神登录失效）：triage 输出 LOGIN_REQUIRED，处方 reopen_login_window + escalate_human。
- 同类故障第二次出现：经验库直接命中，不经 LLM；处置验证通过后置信度 +1。
- LLM 给出白名单外动作：整体拒绝，升级人工（fail-closed）。
- 预算耗尽：不再执行任何动作，升级人工。
