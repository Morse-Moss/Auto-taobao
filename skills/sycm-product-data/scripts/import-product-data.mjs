#!/usr/bin/env node
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import path from 'node:path';
import { spawnSync } from 'node:child_process';
import { loadFeishuCredentials, productDataTargets } from '../../../runtime/feishu-targets.mjs';
import { FeishuClient } from '../../xws-to-feishu-base/scripts/feishu-client.mjs';
import { PRODUCT_HEADERS, planProductImport } from './product-core.mjs';

function args(argv) { const out = { files: [], shop: null, apply: false, evidence: `evidence/product-data-${new Date().toISOString().slice(0,10)}` }; for(let i=0;i<argv.length;i+=1){if(argv[i]==='--file')out.files.push(argv[++i]);else if(argv[i]==='--shop')out.shop=argv[++i];else if(argv[i]==='--apply')out.apply=true;else if(argv[i]==='--evidence')out.evidence=argv[++i];else throw new Error(`unknown argument ${argv[i]}`);} if(!out.files.length||!out.shop)throw new Error('--file and --shop are required');if(out.files.length!==1)throw new Error('one --file per --shop is required');return out; }
function rowsFromXls(file) { const script = path.join(import.meta.dirname, 'read-product-xls.py'); const result = spawnSync(process.env.PYTHON || 'py', ['-3', script, file], { encoding: 'utf8' }); if(result.status!==0)throw new Error(result.stderr||'XLS parser failed'); return JSON.parse(result.stdout); }
function csvLine(row) { return row.map((value) => { const text=String(value ?? ''); return /[",\n]/u.test(text) ? `"${text.replaceAll('"','""')}"` : text; }).join(','); }
async function main(){const options=args(process.argv.slice(2));const raw=rowsFromXls(options.files[0]);if(raw.length<6||raw[4].length!==PRODUCT_HEADERS.length)throw new Error('商品报表缺少 38 列标准表头');const parsed=raw.slice(5).filter((row)=>row.length===PRODUCT_HEADERS.length&&/^\d{4}-\d{2}-\d{2}$/u.test(row[0])).map((row)=>({header:PRODUCT_HEADERS,row,sourceShop:options.shop}));const {appId,appSecret}=loadFeishuCredentials('kcne');const target=productDataTargets('kcne');const client=new FeishuClient({appId,appSecret,appToken:target.baseToken,tableId:target.productTable});const existing=await client.listRecords();const plan=planProductImport({rows:parsed,existing});mkdirSync(options.evidence,{recursive:true});const receipt={source:options.files[0],shop:options.shop,sourceRows:parsed.length,existingRows:existing.length,plannedRows:plan.records.length,mode:options.apply?'apply':'dry-run',manifest:plan.manifest};if(options.apply){const ids=[];for(let i=0;i<plan.records.length;i+=500)ids.push(...await client.batchCreateRecords(plan.records.slice(i,i+500)));receipt.recordIds=ids;receipt.afterRows=(await client.listRecords()).length;}writeFileSync(path.join(options.evidence,'receipt.json'),JSON.stringify(receipt,null,2));console.log(JSON.stringify(receipt,null,2));}
main().catch((e)=>{console.error(`导入失败：${e.message}`);process.exitCode=1});
