Current stage: CLOSE - the authorized test Base's empty SKU detail schema and its reciprocal main-table relationship are applied and verified.
Last completed verified step: `SKU明细.适用空间` is a single-select with only `小户型` and `常规卫生间`; `所属竞品` is a type-21 relation to `竞品主表`, with the automatic `SKU采集明细` backlink. The 2026-08-21 receipt records 1,333 main rows, zero SKU rows, zero record writes, identical main-record fingerprints, and a second dry-run with zero operations.
Next exact action: under separate collection authorization, read the live Feishu formula result for A/B competitors, select the approved product links, and perform a bounded Xiaowangshen SKU capability probe. Use the SKU sizing rule only after real SKU evidence exists.
Scope boundary: this schema stage did not write SKU records, modify existing main-table values, run Feishu AI, or start Xiaowangshen/Taobao collection. Local closeout may commit the reviewed source/tests/pointer snapshot; push, deployment, and SKU collection remain outside this stage.
