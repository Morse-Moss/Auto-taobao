突变验证：runtime/fill-weekly-attribute-labels-core.mjs
跑在 HEAD=49cdd52 之后；目标判据文件 sha256(12)=b658ee948632

基线：exit=0 pass=13 fail=0（全绿，可开始突变）

M1  CAUGHT             重算通道永远开（丢掉 recomputeAb 开关）—— 默认行为会变
      期望点名含：未开重算
      实红：decideWrite：默认（未开重算）在任何有值格上都跳过 —— 只填空不覆盖，与从前逐字相同
M2  CAUGHT             不判 A/B —— 任何行都可重算
      期望点名含：非 A/B
      实红：decideWrite：非 A/B 行、未接上 SKU 的行，一律不得覆盖
M3  CAUGHT             不判列 —— 10 列全都可重算（幂等性没了）
      期望点名含：只填空不覆盖
      实红：decideWrite：默认（未开重算）在任何有值格上都跳过 —— 只填空不覆盖，与从前逐字相同
M4  CAUGHT             不判是否真接上 SKU —— 没数据也敢覆盖
      期望点名含：未接上 SKU
      实红：decideWrite：非 A/B 行、未接上 SKU 的行，一律不得覆盖
M5  CAUGHT             允许把有值擦成 无注明 / 不适用
      期望点名含：擦成无值
      实红：decideWrite：绝不把有值擦成无值，也绝不写空
M6  CAUGHT             允许往表里写空值
      期望点名含：绝不写空
      实红：decideWrite：绝不把有值擦成无值，也绝不写空
M7  CAUGHT             同值也写（丢掉 skip-same）
      期望点名含：逐字相同
      实红：decideWrite：新值与原值逐字相同时不写（省一次无意义更新）
M8  CAUGHT             把 材质分类 也列进可重算列
      期望点名含：RECOMPUTABLE_COLUMNS
      实红：RECOMPUTABLE_COLUMNS：只有 尺寸 与 适用空间 允许被重算 | decideWrite：默认（未开重算）在任何有值格上都跳过 —— 只填空不覆盖，与从前逐字相同
M9  CAUGHT             放宽商品 id 提取（路径里的 /id= 也算）
      期望点名含：extractProductId
      实红：extractProductId：从淘系链接取出商品 id
M10  CAUGHT             把 C 也算进 A/B
      期望点名含：isAbClass
      实红：isAbClass：只有 A- / B- 前缀算「该有尺寸」 | summarizeAbReadiness：只统计 A/B 行，非 A/B 不进队列
M11  CAUGHT             就绪度不筛 A/B —— 所有行都进队列（队列会灌爆）
      期望点名含：只统计 A/B 行
      实红：summarizeAbReadiness：只统计 A/B 行，非 A/B 不进队列
M12  CAUGHT             链接提不出 id 时也去查 SKU 表（还把它当成「有 SKU 数据」）
      期望点名含：两种缺失
      实红：summarizeAbReadiness：两种缺失分开报，链接提不出 id 时不去查 SKU 表
M13  CAUGHT             两种缺失不分开报（全报成 no-sku-data）
      期望点名含：两种缺失
      实红：summarizeAbReadiness：两种缺失分开报，链接提不出 id 时不去查 SKU 表
M14  CAUGHT             就绪数恒等于 A/B 总数（缺口被抹平 ⇒ 闸门永远不拦）
      期望点名含：只统计 A/B 行
      实红：summarizeAbReadiness：只统计 A/B 行，非 A/B 不进队列

还原自证：sha256(12)=b658ee948632  逐字节一致 OK
还原后复跑基线：exit=0 pass=13 fail=0

小结：CAUGHT 14/14，MISSED 0，RED-but-unexpected 0，SKIP/NOOP 0
