# 竞品链当前跑到哪了（2026-09-21 11:2x 实机只读快照）

为什么单独取这组证：用户直接问「竞品链全跑通了吗」。回答不能靠记忆 —— 这个项目已经出现
过「同一批事实在三份文档里写出三个版本」。**本轮不采信任何文档，重跑只读探针实测。**

边界：四个探针**全部只发 GET**（listTables / listFields / listRecords / 浏览器端口探活），
未创建、未更新、未删除任何记录或字段，**未启动或停止任何进程**。

## 文件

| 文件 | 是什么 |
| --- | --- |
| `browser-inventory.txt` | `runtime/browser-inventory.mjs` 只读对账：7 个实例是否就位 |
| `weekly-tables-across-periods.txt` | 四期竞品周表 + SKU 周表 + 问题库的字段类型与有值率 |
| `ab-vs-size.txt` | **关键表**：各期「A/B 行数」vs「尺寸列非空行数」vs「尺寸是 SKU 格式行数」 |
| `queue-weekly.json` | `summarize-xws-sku-queue.mjs --weekly`：按周表当期口径的待采缺口 |

## 结论（一句话）

**没有全跑通。** 跑通的是 08-23 与 09-13 期的**前半段（第 1→6 步）**加一次第 7 步；
后半段（SKU 周表、主表那两列、发布同步）与「本周」都还没走。

## 各期实测

| 期 | 行数 | 字段 | A/B 行 | 尺寸非空 | 尺寸是 SKU 格式 | 尺寸列类型 |
| --- | --- | --- | --- | --- | --- | --- |
| 08-23~08-29 | 1461 | 39 | 8 | 12 | 0 | Lookup(19) |
| 08-30~09-05 | 1423 | 35 | **0** | 0 | 0 | **无此字段** |
| 09-06~09-12 | 1462 | 35 | 1 | 0 | 0 | **无此字段** |
| 09-13~09-19 | 1417 | 39 | 1 | 1417 | 2 | Text(1) |

**口径提醒**：09-13 期「尺寸 1417/1417 有值」**不等于** 1417 行都是真尺寸 ——
其中 1364 行是 `无注明`（非 A/B 行的兜底值），真正 SKU 格式的只有 2 行。
**别把有值率当正确率。**

主表：2004 行，A/B 且有效 **12** 行，`尺寸` 有值 **15** 行（列类型仍是 Lookup(19)）。

## 六处没通的地方

1. **本周（09-20~09-26）的周表根本还没建**：base 里只有 4 张竞品周表，最新是 09-13~09-19。
   即这一周的链一步都没走。
2. **SKU 周表只有 1 张**（`SKU周_2026-08-23_2026-08-29`）；09-13 期**从未创建**。
3. **第 8 步的 SKU 历史同步因此跑不了**：`runtime/sync-weekly-sku-history.mjs:164`
   要求 `SKU周_<周期>` 存在，找不到就 fail-closed（`Weekly SKU table not found`）。
4. **主表 `尺寸`/`适用空间` 仍是 Lookup(19)、15/2004（0.7%）**：平台枷锁（开放 API 建不了/改不了
   Lookup 联接），不是脚本写错。现在只用「周表改 Text」绕过了「数据在不在」，
   **没解决「能不能流进主表那一列」**。
5. **周表 `商品ID` 列 0/1417**：导入那一步没把它写进去。
6. **08-30 与 09-06 两期结构本身残缺**：只有 35 字段，
   `尺寸`/`适用空间`/`数据状态`/`待补数据项` 四列**不存在**，属性列全 0%，A/B 行分别是 0 和 1。

## 唯一必须人的一环

第 7 步（SKU 富化）：买家号登录态。`evidence/sku-step6-2026-09-20/preflight-01.txt` 是
`STALLED` exit 3、商品页被重定向到 `login.taobao.com` ⇒ 只能人扫码。

浏览器不是瓶颈：本轮实测 **7 个实例全部就位**，含竞品链 `9222`/`3457`。

## 尺寸链这一段（今天上午刚做的，已复核）

09-13 期那个唯一 A/B 行（`B-高价值竞品` id `921092099640`）已真写：
`尺寸 = 1.4m,1.4m-1.7m,1.5m,1.6m,1.7m`、`适用空间 = 常规卫生间`。
`queue-weekly.json` 复核 `ab 1 / ready 1 / pending 0`。
（真写过程与独立回读见 `wbrun-apply.txt`、`linkid-join-after-apply.txt`。）
