# 2026-09-17 删除重写：执行前待确认

状态：**待用户确认**。本文件在确认前不代表任何已发生的动作。
生成时间：2026-09-18 20:0x（北京时间）

## 一、只读核到的现场（全部是 GET）

底单（`总数据来源底单`）1880 条，2026-09-17 命中 5 条：

| recordId | 店铺名称 | 本链写的？ | 处置 |
| --- | --- | --- | --- |
| `recvvyuoVc9nXi` | 盖文旗舰店（=盖文天猫） | **不是**（09-14/15/16 那三天也是它，来自单店时代） | **保留** |
| `recvvyR2ueWnn8` | 里可林家居 | 是 | 删 |
| `recvvyRBZ74juL` | 网林家居旗舰店 | 是 | 删 |
| `recvvyS3CjfdVy` | 盖文全卫定制 | 是 | 删 |
| `recvvySuZ5zUpB` | 科塔全卫定制 | 是 | 删 |

**为什么保留盖文旗舰店那一行**：它的数据只能从「登录成盖文旗舰店」的浏览器里重采，而本机现在五个浏览器里
没有一台是它（19022/19023 实测登录的是里可林家居）。删了就补不回来 —— 所以它不是「本轮要重写的那部分」。

各店铺数据日报（`各店铺数据日报`）2197 条，2026-09-17 命中 **12 条**（每天每店一行）。
33 个字段里 28 个是派生列（Lookup/Formula），只有 5 个手写列：`日期` / `店铺` / `补单金额` / `询单量` / `同层同行询单量`。
经核查：`补单金额` 12 行全空，除本链两个字段外没有别的手写内容。

**12 行一行都不删** —— `run-inquiry-backfill.mjs` 只匹配已存在的行，没有创建行的通路，删行等于让回填无从下手。

## 二、拟执行的动作（不可撤销）

1. 删底单里上面那 4 行（单条 DELETE；`batch_delete` 在这张 base 上报 1254043，见 sop §9.3）。
2. 清掉那 4 家「各店铺数据日报」里的 `询单量` / `同层同行询单量` 共 8 个格子（行保留）。
   当前值是 里可林 3/6、网林天猫 4/13、盖文淘宝 4/6、科塔淘宝 5/6，清掉后由回填重新写。

   为什么要清：不清的话回填会判 `ALREADY_VERIFIED`（值已经对），于是「全流程」里那一次真实写入不会发生。
   清掉的代价很低 —— 那两个值可以从生意参谋页重算（目标日 09-17 就是昨日，预设模式拿得到同行基准）。
3. `run-multi-shop-day.mjs --date 2026-09-17 --commit` 真写四家店（11 阶段 × 4 家）。

预期落位：底单 1880 → 1876 →（四家各 +1）1880，2026-09-17 仍是 5 行。

## 三、脚本自带的安全判据（任一不成立即整体停手，不做部分执行）

- A：底单 09-17 里「盖文旗舰店」必须恰好 1 行；
- B：待删行数必须恰好等于本轮要重写的店铺数（4）；
- C：各店铺数据日报 09-17 必须恰好 12 行；
- D：除本链两个字段外，不得有其它手写内容（有人记过东西就停手问人）；
- E：目标四行必须都在。

干跑（不带 `--apply`）已通过全部 5 条，快照落在 `pre-delete-snapshot.json`。

## 四、明确的边界

- 不动盖文天猫 2026-09-17 的数据（底单那一行 + 询单 13/36 都保留）。
- 不动其余 7 家店（保拉淘宝/保拉天猫/网林淘宝/里可林天猫/安比龙头店/科塔龙头店/安比淘宝）。
- 不动 09-14 / 09-15 / 09-16（各 1 行，都是盖文旗舰店）。
- 本地审计表刻意不回改：它现在指向已被删掉的 recordId，这正是「审计允许与飞书不一致」要暴露的现象。
