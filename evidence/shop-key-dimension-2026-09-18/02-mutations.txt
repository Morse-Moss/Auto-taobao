[M1 目录键被忽略（键给了也不落到目录名上）]
  改坏后：pass=15 fail=1 退出码=1
  点名用例：证据目录的店铺维度：不给键时逐字不变，给了键就加一维
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：sha f77a81ea4ae0 → f77a81ea4ae0（逐字节一致） pass=16 fail=0
[M2 字符集放宽到「什么都放行」（斜杠/空格/控制字符都能当目录后缀）]
  改坏后：pass=15 fail=1 退出码=1
  点名用例：证据目录的店铺键：非法键一律抛错，不许被拼进路径
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：sha f77a81ea4ae0 → f77a81ea4ae0（逐字节一致） pass=16 fail=0
[M3 错标签只记不拦（把「抛错」降级成「不核对」）]
  改坏后：pass=13 fail=1 退出码=1
  点名用例：证据目录的店铺键：错标签一律抛错并点名两边（原样比，不做相似度猜）
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：sha c3a9de495f42 → c3a9de495f42（逐字节一致） pass=14 fail=0
[M4 回读方不再把 --shop-key 传下去（自己拼一个不带店铺维度的目录）]
  改坏后：pass=15 fail=1 退出码=1
  点名用例：三个写入方都按同一维拼证据目录，并且都把店铺键交给了它
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：sha e5f1b6d8b266 → e5f1b6d8b266（逐字节一致） pass=16 fail=0
[M5 回读方不再限制键（任意字符串都能当目录后缀）]
  改坏后：pass=15 fail=1 退出码=1
  点名用例：三个写入方都按同一维拼证据目录，并且都把店铺键交给了它
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：sha e5f1b6d8b266 → e5f1b6d8b266（逐字节一致） pass=16 fail=0

结论：5 条突变各自点名了它该点名的那条用例，还原后全部复绿。
