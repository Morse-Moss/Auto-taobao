[基线]
  两个测试文件共 pass=48 fail=0 退出码=0
  sha(report)=e7f46a172536 sha(readback)=7e07458da8d6 sha(page)=0fecdcfbc8c0 sha(promo)=890f474e223f

[M1 删掉 main 里的落位这一步]
  改坏后：pass=47 fail=1 退出码=1
  点名的用例：接线：push 的 main 先落位、再复核（删掉 ensureTargetPage 这一行必红）
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：逐字节一致=是 pass=48 fail=0 退出码=0

[M2 落位里不再发导航请求]
  改坏后：pass=47 fail=1 退出码=1
  点名的用例：ensureTargetPage：这一页停在别张表上时，进场自己导航回源表（导航在复核之前）
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：逐字节一致=是 pass=48 fail=0 退出码=0

[M3 归位搬出 finally，退回成功路径]
  改坏后：pass=47 fail=1 退出码=1
  点名的用例：接线：readback 的归位写在 finally 里，且失败时自己吞掉（不能顶掉原始错误）
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：逐字节一致=是 pass=48 fail=0 退出码=0

[M4 停错表的诊断丢掉「共用页」这条线索]
  改坏后：pass=47 fail=1 退出码=1
  点名的用例：describeTargetMismatch：同时报出「当前」与「期望」，并点出这一页是上一家留下的
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：逐字节一致=是 pass=48 fail=0 退出码=0

[M5 去掉「点不着就滚进视口」这一步]
  改坏后：pass=47 fail=1 退出码=1
  点名的用例：等「确定」：点不着时先把它滚进视口，滚完按原判据重判（不是干等）
  期望的那条是否红了：是 —— 判据真的在判东西
  还原后：逐字节一致=是 pass=48 fail=0 退出码=0
结论：5 条突变各自点名了它该点名的用例，还原后逐字节一致且全部复绿。
