# 执行：把 09-17 补成五家齐（用户 2026-09-19 14:1x「按你推荐的来」）

承接同目录 `00-README.md` §五「可恢复的路径」。那份写的是**当时还没做**的清单，
这一份记的是**做了之后**的实测结果。

授权原文（用户）：`按你推荐的来`。
我据此报的两条推荐是：① 补 09-17（先写回三家缺的同行格，再单独跑盖文淘宝 09-17）；
② 查 19043 早上为什么登成了盖文旗舰店但**不动它**。

## 一、先关掉挡住整页的全屏弹窗（不然补采第一步就死）

- 现象（上一轮 06:30 那次失败的原文，见 `evidence/multi-shop-2026-09-17/盖文淘宝/03-promotion-submit.txt`）：
  `「下载报表」复核未通过（not-hit，命中自己=0，遮挡物={"tag":"DIV","cls":""}）`。
- **关键新事实：这类弹窗的 id 会变。** 09-19 04:43 那次是 `#wrapper_dlg_982`
  （`data-owner-id=universalBP_tool_auto_dlg`「优质计划防停投」）；
  这次同一台机器同一站点已经是 `#wrapper_dlg_925`（`data-owner-id=app`），
  里面并排着「立即报名」和「关闭」两个按钮。
  ⇒ 沿用硬编码 `#wrapper_dlg_982` 的探针（`93`）会报 `present:false` 然后 exit 0，
  让人**误以为「没有弹窗」**。这就是我没有直接跑 `93` 而先做只读盘点的原因（探针 `129`）。
- 关掉它的做法（探针 `130-close-overlay.mjs`）：不认 id，按「盖住视口 ≥85% 宽高、且 z-index 最高」
  认定遮挡层，在该层内只取「中心点命中自己」的小元素当候选，并带**危险词黑名单**
  （报名／开通／购买／支付／确认／提交／领取／升级／续费／立即 ⇒ 命中者永不被选中，
  否则盲点会点到「立即报名」这种有对外副作用的按钮），优先选文案含「关闭」的。
- 结果（默认只读 → 确认选点 → `--apply`）：

  ```
  pick = SPAN cls="asiYysqqaU " text="关闭" 于 (569,634)      excludedAsDanger = ["立即报名"]
  点击：HTTP 200 {"clicked":true,...,"x":569,"y":634}
  回读：remainingFullscreenLayers=[]   downloadButton={ok:true, by:"SPAN"}
  ```

  回读两条都过才算成功（点击返 200 不算数）。

## 二、盖文淘宝 09-17 重采（11 阶段全过）

命令：`run-multi-shop-day.mjs --date 2026-09-17 --shops 盖文淘宝 --commit --allow-missing-peer
--logs evidence/multi-shop-2026-09-17-retry1`（**另起日志目录，保留上一轮失败证据**）。

- 身份自证：页头店名 `盖文全卫定制`、会员 `随心品质定制:阿彦` `887360146` —— 与登记表一致。
- 体检：阻断 0、告警 1（`EGRESS_PROXY_NOT_DECLARED`，已知）；未实现的层仍如实标 `NOT_IMPLEMENTED`。
- **push**（`07-push.txt`）：`recordId=recvvE4MrW0uJI`，底单 **1884→1885**，
  `verifiedFields=240`，源 `盖文全卫定制`，`targetOnlyPromotionFieldsBlank=true`；审计 **id=44**。
  stderr 两条 [留空] ＝源侧给文本 `NULL`（`UV价值`、`无线端UV价值`）⇒ 数字列留空，不是 0。老坑，非新问题。
- **backfill**（`10-backfill.txt`）：写 `询单量=4`，`unchangedOtherFields=true`；审计 **id=45**。
  **同行格按设计留空**（`--allow-missing-peer` 降级路径：目标日已不是「昨天」⇒ 同行同层行取不到，
  记 `degraded.code=PEER_UNAVAILABLE`，不写 0）。

## 三、三家缺的同行格：从快照写回（不是重采）

脚本 `evidence/daily-report-2026-09-17-recover/script-recover-0917-cells.mjs`
（六条 fail-closed 判据：默认 dry-run／目标行恰好一行／record_id 与快照一致／
**只补空白绝不覆盖**／写后回读两格相等且除这两列外逐字不变／快照值必须齐全）。

| 店 | 现值 | 快照 | 写回 | 审计 id |
| --- | --- | --- | --- | --- |
| 里可林淘宝 | 询单 3 / 同行 null | 询单 3 / 同行 **6** | `同层同行询单量=6` | 41 |
| 网林天猫 | 询单 4 / 同行 null | 询单 4 / 同行 **13** | `同层同行询单量=13` | 42 |
| 科塔淘宝 | 询单 5 / 同行 null | 询单 5 / 同行 **6** | `同层同行询单量=6` | 43 |

三家都是 `WRITE_PEER_ONLY`（现值与快照的「询单量」一致、只缺同行格），
mode 记 `recover-from-snapshot`。收据 `recover-receipt.json`。

## 四、独立核对（换一条通路读同一个事实；探针 `131`、`132`）

- **底单 09-17 = 5 行**：盖文旗舰店(recvvyuoVc9nXi) / 里可林家居(recvvCR5c7lDoe) /
  网林家居旗舰店(recvvCRwHGUZYL) / 科塔全卫定制(recvvCRW7h7a5l) / 盖文全卫定制(recvvE4MrW0uJI)。
  （底单店名＝**源页头名**；「盖文旗舰店」就是盖文天猫那一行，与询单表的选项名不同是正常。）
- **询单表 09-17 = 12 行，我方五家齐**：盖文天猫 13/36、里可林淘宝 3/6、网林天猫 4/13、
  盖文淘宝 **4 / 同行格空**、科塔淘宝 5/6。
- **审计链全表复核**（`132`，44 行 id 2…45，**无 id 空洞**）：9 处断点**逐条可解释**，没有一处是「偷偷删行」：

| 断点 | 解释 |
| --- | --- |
| id 2/3/4 → 6，各差 -1 | 单店时代的**副本 base**（8 行），每轮重写同一天，账目口径与正主不同 |
| id 8(8) → id 10(1873)，差 +1865 | **换 base**：副本 8 行 → 正主 1873 行 |
| id 16(1880) → id 21…24(1879) | `ui-verify` 的 before/after 记的是**传入的期望值**（`--expected-before-count`），不是实测，与 push 不可比 |
| id 24(1880) → id 25(1876)，差 **-4** | **真实的删行**：09-19 09:33 前那次「清一天重写」，即本目录 §三 的成因 |
| id 27(1879) → id 44(1884)，差 +5 | 09-19 12:36~13:49 写**报表日 09-18** 的五家（审计 id 31/33/35/37/39），属别日写入 |

## 五、收口后的状态

- 09-17：**底单 5 行、询单表五家齐**。唯一空格＝盖文淘宝的「同层同行询单量」，
  **按设计如此**（跨过午夜同行基准永久丢失，降级留空而不是写 0）。
- 09-18：五家齐（见 `00-README.md` §二）。
- 09-17 由「4 行、缺一家」变成「5 行、五家齐」；本目录 §三 那句「恢复之前不要把 09-17 当成五家齐」
  **现在可以撤销**：它已经是五家齐，只余一格按设计为空。

## 六、这一轮**仍然没做**的事（别当成已完成）

- **没把「开工作页 + 关全屏弹窗」从仓库外探针搬进仓库/驱动**（现住 `86`/`90`/`93`/`109`/`128`/`130`）。
  若让 `promotion-submit` 自己发现并关掉遮挡层，属于改仓库代码 ⇒ 需配突变验证，另开一轮做。
- **`run-inquiry-backfill.mjs` 仍不接受身份判据**（`--expect-shop`/`--expect-member`）——
  既有缺口，未修。
- **残留页签未清理**（19043 上的千牛后台页、其它台的 `about:blank`）—— 需明确许可。
- **定时未挂**；**09-19 这一天的采集**按口径应等 09-20 跑。
