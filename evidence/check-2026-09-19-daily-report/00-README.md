# 体检：2026-09-19 收集到的数据有没有问题（只读，未改任何东西）

用户 2026-09-19 14:0x 的话：「先给我检查一下今天收集到的数据有没有问题」。
「今天收集到的」＝**报表日 2026-09-18** 那一批（今天 12:36~13:49 写入），
外加今天上午 09:31~09:41 那轮**重写 09-17** 的动作。

## 一、做法（四层，全程只读；没开浏览器、没导航、没关页签）

1. **审计表** `daily_report_push_audit`（库 `xws_automation`）：把 `record_count_before/after`
   按 id 连成链、逐条看 `outcome`、`verified_fields`、`browser_port`/`browser_id`。
   探针 `D:/Retire/probe-live/117-audit-detail-0918.mjs`。
2. **回执**：五家 `evidence/daily-report-2026-09-18-*/receipt.json` 逐份比对字段集合与自证段。
   探针 `118`~`123`。
3. **源**：把今天那五份工作簿（`C:\Users\Administrator\Downloads\日报*20260919*`）与推广 ZIP
   用仓库自己的 `extract-sources.py` 重解析，**逐列对比五家取值**（这是区分「源里就空」与「管道丢了」的唯一办法）。
4. **飞书全表**（最强、且不需要浏览器）：`FeishuClient.listRecords()` 拉全量
   （底单 1884 / 询单表 2197）＋ `getRecordCount()` 交叉，逐日行数、重复行、逐格空值。
   探针 `124-feishu-integrity-scan.mjs`、`125-inquiry-values-0917-0918.mjs`。

## 二、结论 A：09-18（今天收的那批）**没有实质问题**

- **底单** 09-18 恰好 5 行，五家各一：盖文旗舰店 / 盖文全卫定制 / 里可林家居 / 网林家居旗舰店 / 科塔全卫定制。
- **询单表** 09-18 五家：盖文天猫 **12/35**、里可林淘宝 **4/6**、网林天猫 **6/12**、盖文淘宝 **6/6**、科塔淘宝 **5/6**。
- **抽样串校验**：底单值与源一致 —— 盖文天猫 访客数 342 / 支付金额 0 / 关键词推广花费 245.53（＝推广 CSV 花费）；
  网林 支付金额 8558 ＝ 源 8,558.00。
- **`verified_fields` 240/241/241/243 不等不是 bug**：它是「实际写进去几格」，差异**全部由源侧空格解释**：

| 店 | 空/特殊格 | 原因（源侧） |
| --- | --- | --- |
| 里可林淘宝 | `UV价值`、`无线端UV价值` 空 | 源给文本 `NULL`，数字列存不下 ⇒ 留空＋warn（老坑） |
| 盖文天猫 | 同上 2 格空 | 同上 |
| 科塔淘宝 | 同上 2 格空；另 6 个转化率列存成字面 `NULL` | 源给 `NULL`；转化率列是**文本**类型，原样存得下（与 `PC端UV价值` 同族） |
| 网林天猫 | `总收藏加购成本`/`宝贝收藏加购成本`/`宝贝收藏成本`/`加购成本` 4 格空 | 它当天**收藏＝0、加购＝0**（推广行 `总收藏加购数=0`）⇒ 源里就是空，不是漏写 |
| 盖文淘宝 | 一格不缺 | —— |

- 网林回执里多出的 `店铺收藏成本 (1)`＝80.61，出自**人群推广行**（只有它 `收藏店铺数=1`）——
  底单里有个重名残留字段 `店铺收藏成本 (1)`，映射靠 `canonicalPromotionTargetName` 抹掉 ` (1)` 后缀才配上。正常。

## 三、结论 B：**09-17 有个真洞**（今天上午那轮「清一天重写」留下的）

- **底单 09-17 现在只有 4 行**：盖文旗舰店 / 里可林家居 / 网林家居旗舰店 / 科塔全卫定制
  —— **缺「盖文全卫定制」＝盖文淘宝那一行**。
- **询单表 09-17**：盖文淘宝那行 `询单量`/`同层同行询单量` **两格都空**；
  里可林淘宝/网林天猫/科塔淘宝 只有 `询单量`（3/4/5），**同行格空着**。
- 成因（链条完整可查）：
  1. `evidence/daily-report-2026-09-17-clear/PLAN-PENDING-CONFIRMATION.md` 的原计划＝
     「删底单 4 行 ＋ 清 4 家 8 格 ⇒ 重跑四家 ⇒ 底单回到 5 行」，并写着「清掉的代价很低，重算得回来」。
  2. 实际重跑是 **09-19 09:31~09:36**（`evidence/multi-shop-2026-09-17-commit/`），
     **盖文淘宝死在 `promotion-submit`**：19043 阿里妈妈页读到 `盖文旗舰店:阿彦`（期望 `随心品质定制:阿彦`）
     ⇒ 身份判据停手，它没走到 push；
     **里可林/网林/科塔 死在 `backfill`**：目标日 09-17 已不是「昨天」⇒ 同行同层行取不到。
  3. 于是只有 3 行被补回（审计 id 25/26/27），底单 09-17 = 4 行；三家的同行格走降级路径空着。
- **教训**：原计划的前提（「目标日就是昨日，同行基准重算得回来」）**只在当晚成立**。
  「清一天重写」是有时效的操作：跨过午夜，被清掉的同行基准**永久丢失**。

## 四、结论 C：历史遗留（与今天无关，报的时候要说清）

- 底单 `2026-06-09 | 安比定制家居` ×2、询单表 `2026-06-03 | 安比淘宝` ×2 —— 各一组重复行。
- 底单 09-14/09-15/09-16 各只有 1 行（单店时代，都是盖文旗舰店）；09-10~09-13 底单无行（用户已定「不用补」）。
- 询单表里 7 家（保拉天猫、保拉淘宝、安比龙头店、安比淘宝、网林淘宝、里可林天猫、科塔龙头店）
  字段本来就空 —— 不在「只收五家」口径内，不是漏跑。

## 五、可恢复的路径（**都还没做，等授权**）

被清掉的值有快照：`evidence/daily-report-2026-09-17-clear/post-delete-verification.json`
记着 里可林 **3/6**、网林天猫 **4/13**、盖文淘宝 **4/6**、科塔淘宝 **5/6**。

- 三家「同行格」：值不必重采，写回即可（写飞书＝需要明确授权）。
- 盖文淘宝 09-17：底单行要**重新采一次**（新导 09-17 的生意参谋日报 + 推广报表，走 `--allow-missing-peer`），
  两个格子随之由回填写回。
- 恢复之前，**不要把 09-17 当成「五家齐」**。

## 六、这次没做的事

- 没写任何飞书数据、没改任何代码、没跑任何 `--commit`、没导航或关闭任何页签、没重启任何进程。
- 判据/代码本轮零改动；本文只是体检记录。探针全部落在仓库外 `D:/Retire/probe-live/`（`117`~`125`）。
