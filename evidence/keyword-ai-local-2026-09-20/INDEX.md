# evidence/keyword-ai-local-2026-09-20

「把 AI 字段改成**本地跑**」这件事的**前置盘点 + 第一步落地**：先只读盘清现状，再决定动什么。
本目录的 01~06 是**纯只读盘点**（写下这份时对飞书零写入；规则写入器唯一一次调用是 dry-run，
它自己 fail-closed 退出了）。**在此之后**，规则段补完缺失选项、**真跑写入并独立回读通过** —— 见文末
「规则段：本轮的写入与独立回读」。**AI 段（内容热度）至今未写任何东西**。

对应表：`关键词分析 V1（2026-09-19）` = `tblZsUns9353w3nl`（base `HdBhbttB5aScbasWJAMc0gGXnpe`，租户 kcne）。
对照表：`关键词分析 V1（2026-09-12）` = `tblrX0GM7HkVhF85`（上周，已定稿，只读用）。

## 一句话结论

关键词周表的「分析字段」在飞书里**没有一个是 AI 字段**：12 个是公式，17 个是普通可写字段。
所以「本地跑 AI 字段」实际要做的是**两件不同的事**：

1. **规则段**（4 个字段：标准归并词 / 关键词分类 / 细分标签 / 用户意图）—— 本地代码**已经存在**，
   且有一条 2026-08-16 的成功先例；本周被**一个缺失的下拉选项**挡住（见 03），补完选项后**已真跑写入并独立回读通过**。
2. **AI 段**（1 个字段：内容热度）—— **本地没有接进现行周更链**，但仓库里有一条**完整可跑的老 AI 段**
   （`run-legacy-feishu-prompt-analysis.mjs`，走本机 codex/claude，**不需要外部 key**）；
   它不能整条照搬，因为 5 个写回字段里 4 个已被规则段覆盖、`对应产品方向` 已变成公式。见 10 号文件。

## 表结构实测（`runtime/../read-keyword-table-fields.mjs`）

| 类别 | 数量 | 字段 |
| --- | --- | --- |
| 公式（type=20，**不可写**） | 12 | 交易热度 / 搜索热度 / 来源渠道 / 原始关键词 / 主关键词 / 一级类目 / 优先级 / 近2周重点达标次数 / 近2周探索达标次数 / 近2周A级达标次数 / 是否重点词 / 对应产品方向 |
| 普通可写 | 17 | 排名 / 搜索词 / 搜索人气 / 点击率 / 支付转化率 / 关键词编号 / 标准归并词 / 关键词分类(3) / 细分标签(4) / 用户意图(3) / **内容热度(1)** / 采集日期(5,auto) / 已有有效批次数(2) / 灰豚话题浏览量(2) / 上一有效周重点达标(2) / 上一有效周A级达标(2) / 上一有效周探索达标(2) |

**AI 字段（type 带 ai/prompt 属性）＝ 0 个。** 上周表（`tblrX0GM7HkVhF85`）字段签名与本周**逐字相同**，
同样 0 个 AI 字段 ⇒ 克隆没有丢东西。

## 关键的一点：`.workbuddy` 里那句「AI 字段」指的是谁来算

- `docs/project-knowledge.md:74`：「`1000w` 只作为灰豚浏览量参与 A 条件的门槛；**内容热度由上游内容平台/飞书 AI 提供**，灰豚不把浏览量转换成内容热度。」
- `README.md`：`内容热度` 由上游流程提供。
- ⇒ 「本地跑 AI 字段」＝ 把「上游（飞书 AI / 内容平台）」换成**本地模型**。这是**改口径**，不是修 bug。
  （**订正**：这个「上游」在历史上其实**也是本地跑的**，见 10 号文件 —— 老 AI 段
  `run-legacy-feishu-prompt-analysis.mjs` 的 provider 是本机 codex/claude CLI，不是飞书 AI。
  「上游」指的是「周更链的上游环节」，不是「云端」。）

## 填充度实测（`read-keyword-table-fill.mjs`）

| 字段 | 本周 `tblZsUns9353w3nl` | 上周 `tblrX0GM7HkVhF85` |
| --- | --- | --- |
| 标准归并词 | **0 / 300** | 300 / 300 |
| 关键词分类 | **0 / 300** | 300 / 300 |
| 细分标签 | **0 / 300** | 282 / 300 |
| 用户意图 | **0 / 300** | 300 / 300 |
| 内容热度 | **0 / 300** | 300 / 300 |
| 灰豚话题浏览量 | **0 / 300** | **0 / 300**（上周也没跑灰豚） |
| 上一有效周重点达标 / A级达标 / 探索达标 | **0 / 300** | 300 / 300 |
| 搜索热度 / 交易热度（公式） | 300 / 300 | 300 / 300 |
| 是否重点词 / 优先级（公式） | 300 / 300，值全是 `待数据` | 300 / 300，有真值 |
| 对应产品方向（公式） | 300 / 300，值全是 `暂无` | 300 / 300，有真值 |

⇒ 公式那一侧**是好的**：`待数据`/`暂无` 不是公式坏了，是它依赖的可写字段全空（这是设计上的正确表现）。

## 公式依赖图（`read-keyword-table-formulas.mjs` 读回的原文）

```
搜索热度     ← 搜索人气
交易热度     ← 支付转化率
优先级       ← 关键词分类 / 细分标签 / 搜索热度 / 交易热度 / 内容热度 / 灰豚话题浏览量
是否重点词   ← 搜索热度 / 交易热度 / 近2周重点达标次数
近2周X达标次数 ← 上一有效周X达标 + 当期(搜索热度/交易热度/内容热度/优先级)
对应产品方向 ← 近2周重点/A级/探索达标次数      ← 注意：这一列是**规则**，不是 AI
```
⇒ 本地要产出的可写字段只有 5 个：`标准归并词`、`关键词分类`、`细分标签`、`用户意图`、`内容热度`；
其余 5 个（`是否重点词`/`优先级`/`对应产品方向`/`搜索热度`/`交易热度`）**已经是公式，本地不该再算一遍**，
否则写进去会被 API 拒或被公式覆盖 —— 这也是孤岛 artifact 不能直接喂给 `run-weekly-post-ai.mjs` 的原因
（它的 `publishPlan.current.updates` 带 10 个字段，其中 5 个在本表里是公式）。

## 已在仓库里、且被验证过的「规则段」实现

- `runtime/local-keyword-analysis.mjs`：纯规则引擎，`TARGET_FIELDS = ['标准归并词','关键词分类','细分标签','用户意图']`。
- `runtime/apply-local-keyword-analysis.mjs`：写入器。带**变异白名单**（只允许对确认过的表 batch_update 这 4 个字段）、
  **canary 单条先写 + 字段定义不得变化 + 全量回读比对**，收据状态 `APPLIED_AND_VERIFIED`。
- 成功先例：`runtime/local-analysis-runs/20260816T061513Z-tblCswWXxEVGV20s/receipt.json`
  → 表「关键词分析 V1（2026-08-15）」300 行 / 1200 次字段写入 / `frontendRecordsPending: 0`。
  ⇒ 这条路**不是新造的**，是已有能力，本周只是没接上。

## 本地 provider 可用性（`04-provider-cli.txt`）

| provider | `where` 结果 |
| --- | --- |
| `claude`（cc） | FOUND `E:\AI\npm-global\claude.cmd` |
| `codex` | FOUND `E:\AI\npm-global\codex.cmd` |
| `workbuddy` | **NOT FOUND** ⇒ 代码里这个 provider 选项在本机是死的 |

## 文件对照

| 文件 | 结论 |
| --- | --- |
| `01-table-fill-state.txt` | 本周/上周逐字段填充度 + 样本行 + 排名 1..300 唯一 |
| `02-label-option-diff.txt` | 规则引擎产出 vs 字段选项的差集：**只差 1 个选项** |
| `03-rule-dryrun-BLOCKED.txt` | 规则写入器 dry-run 的**失败原文**（fail-closed，零写入） |
| `04-provider-cli.txt` | 三个 provider 的可执行性实测（`workbuddy` 是死选项） |
| `05-content-heat-sample.txt` | 本地 `cc` 在 10 行真实数据上的小样：**能跑**（24.7s / 0 非法），但**质量口径偏松**，不要直接上 300 行 |
| `06-schtasks-blacklist.txt` | `schtasks.exe` 被拦的**原始横幅**（回答「为什么是黑名单」） |
| `07-mutation-readback.txt` | 收据 `readbackText` 判据的**突变验证**（3/3 被点名拦住 + 逐字节还原） |
| `08-content-heat-value-history.txt` | 7 张批次表 `内容热度` 的**全量落库值分布**（回答「之前的规则落库长什么样」） |
| `09-analysis-field-options.txt` | 两张表的分析字段**类型与选项全文**（含公式原文；确认 `内容热度` 是 type=1 Text） |
| `10-content-heat-rule-trace.txt` | 内容热度口径三层追溯 + **订正我上一轮的错误说法** |
| `11-legacy-prompts-and-current-scope.txt` | **老提示词 F 全文** + A~G 七项在现表的下落 + 口径**分成四份**的现状 |
| `12-content-heat-30row-sample.txt` | 我按老提示词 F 判的 **30 行样本**（含规则读法与 4 处边界行标注） |
| `13-sample-vs-lastweek.txt` | 同一批词、**两个执行者逐行比**：22 个可比词里 6 个不一致，`高级浴缸` 低↔高反转 |
| `14-content-heat-structural-forecast.txt` | 结构预演：规则 2 把「中」写成默认值 ⇒ **全表 76% 必然落中**（区分度仅约 24%） |
| `15-content-heat-writer-and-dryrun.txt` | 内容热度**写入器**（只写一个字段）+ 第一次 dry-run + 当场抓出的第二个「收据产假信息」缺陷 |
| `16-mutation-content-heat.txt` | 写入器 5 条判据的**突变验证**（5/5 被点名拦住 + 逐字节还原） |
| `17-content-heat-rule-reproducibility.txt` | 拿 **1200 行历史落库值**当标注集反推老口径：留一批交叉验证 **86.7%**（对照恒判「中」70.3%）⇒ 老口径**是可复现的**，分歧在「读法没钉住」；**订正 13 号文件的结论** |
| `18-content-heat-prompt-literal-alignment.txt` | 现行提示词**逐句落到列上**（无挑选）在同样 1200 行上命中 **88.9%** ⇒ **三个「分歧」原文全写过了，不用人拍**；300 行产物过写入器 dry-run（飞书零写入）；**订正 17 号把分歧摆成「要你拍」的框架** |
| `19-content-heat-is-ai-not-huitun.txt` | 回答「内容热度是不是去灰豚取」：**不是**。合同第 17 行 = AI 预测内容创作潜力；灰豚只产 `灰豚话题浏览量` 且**只读** `内容热度`（写入白名单里没有它）；公式里二者是 AND 并列独立输入，顺序是 内容热度 → 灰豚。五处原文互相独立 |
| `login-buyer-2026-09-20/` | 竞品链买家号登录页已拉起（另一个交付项） |

## AI 段（内容热度）：本轮落地的写入侧

代码：

- `runtime/content-heat-apply.mjs` —— 字段合同 / 写入计划 / 变异白名单 / 回读校验 / 直方图
- `runtime/apply-content-heat.mjs` —— CLI，**默认 dry-run**，真写要 `--apply` + 两个精确确认
- `runtime/content-heat-apply.test.mjs` —— 11 条判据
- `runtime/feishu-readback.mjs` —— 读回归一，**从 `apply-local-keyword-analysis.mjs` 抽出**，两份写入器共用
  （对外接口不变：`apply-local-keyword-analysis.mjs` 改为 import + re-export，原测试仍全绿）

守卫（每条都有判据 + 突变验证，见 16 号文件）：
只写一个字段｜值域四值且**显式拒绝 `AI预测-` 前缀**｜type 必须可写｜覆盖缺口 fail-closed
（缺行/外来 record_id/重复 record_id/产物表号不符全抛）｜默认只填空白格｜写后回读既证「写进去了」
也证「不该动的没动」（全部 type=20 公式列为允许变化）。

状态：**写入侧已就绪；分析侧已按现行提示词判完 300 行并通过写入器 dry-run（`coveredAllRecords: true`、计划分布 中200/低60/高40）。飞书至今零写入。**
产物（仓库外，待授权后才写）：`D:/Retire/probe-live/content-heat-artifact-300.json`；dry-run 落点 `D:/Retire/probe-live/content-heat-runs/`。

## 规则段：本轮的写入与独立回读（不在本目录，指向落点）

- 收据：`runtime/local-analysis-runs/20260920T025633Z-tblZsUns9353w3nl/receipt.json`
  → `APPLIED_AND_VERIFIED`，`recordsVerified: 300`、`fieldsWritten: 1200`、`preservedExisting` 全 0
  （写 4 个字段：标准归并词 / 关键词分类 / 细分标签 / 用户意图；`内容热度` 未写）。
- 写入器本轮改了一处**证据质量**缺陷：公式字段（type=20）经 API 读回是对象，
  旧代码 `String(...)` 会让收据里的 `priorityDistribution` 变成 `{"[object Object]": 300}`
  —— **看着像有数据、实际什么都没说**，比缺字段更坏。已加 `readbackText()` 归一 + 两条判据，
  并用突变验证证明判据会红（`07-mutation-readback.txt`）。
- 独立回读（不采信自报）结论：填充度 标准归并词 300 / 关键词分类 300 / 细分标签 286 / 用户意图 300；
  选项越界 0；与历史批次 8 交叉核对原始列与采集日期**不一致 0 条**；排名 1..300 唯一 300。

## 内容热度是 AI 预测，不是去灰豚取的（19 号文件）

用户问「内容热度这个要去灰豚获取的吧」。答：**不是**，五处独立原文一致：

- `runtime/keyword-field-contract-20260809.md:27`（原本规则）：第 17 行 `内容热度 | AI 预测 | 输入 原始关键词/分类/标签/意图 | 只预测内容创作潜力 | 输出必须带 AI预测- 前缀，不冒充真实平台热度`。
- 提示词 F 首句：「这里没有内容平台的真实热度数据，只能预测内容创作潜力」；规则 4「不得把该结果描述为小红书、抖音、知乎或其他内容平台的真实热度」。
- `runtime/keyword-formula-ai-verification-20260809.md:83`：只允许作为 AI 内容潜力预测。
- 灰豚技能自己的边界（`skills/huitun-to-feishu-keyword-heat/SKILL.md:100/101/111`）：**不去折算**内容热度、内容热度**只读**、写 `内容热度` 会被判**未授权**（写入白名单只有 `灰豚话题浏览量`）。
- 飞书公式（`runtime/keyword-decision-formulas.mjs:76/83/84`）：`内容热度` 与 `灰豚话题浏览量` 是 `A条件` 里 **AND 并列的两个独立输入**；`A候选`（灰豚的输入队列）＝前三个条件已满足且灰豚仍空 ⇒ **顺序是 内容热度 → 灰豚**。

误解来源两处，都是措辞：两个字段在表里并排（第 11 列 / 第 14 列）且都带「热度」；
`docs/project-knowledge.md:50` 原有一句「内容热度尚未**采集**」（漂移用词，已改「尚未生成」）。

## 口径是可复现的：1200 行反推的结论（17 号文件）

**18 号文件纠正了本节的框架**：17 号把三处边界摆成「要你拍」，那是错的 —— 现行提示词（`weekly-local-analysis.mjs:16`）
比老提示词 F 具体得多，「纯品牌→低」「搜索或交易**为高**才判高」「过度宽泛→低」三句全写在原文里。
18 号把原文逐句落到列上（**无任何挑选**），在同样 1200 行上命中 **88.9%**，比 17 号「64 选 1」的上限 88.4% 还高。
⇒ **不需要任何人做判断，三个分歧都不是分歧。** 下面 17 号的描述只作为历史过程保留。

12/13/14 号文件的说法是「规则 2 把中写成默认值 ⇒ 这列区分度只有约 24%」。**17 号文件把这个说法钉细了，也订正了一处。**

拿 base 下 4 个**已落库**批次（08-26 / 08-29 / 09-11 / 09-12，各 300 行）当标注集，穷举提示词 F 的读法：

- 交叉验证（留一批，在 3 批上挑读法、判第 4 批）平均命中 **86.7%**，恒判「中」是 70.3%
  ⇒ 历史落库值背后**存在一个可复现的判定函数**。所以问题是「读法没被钉住」，不是「判定随机」。
- 三处分歧的数据结论：
  - 「信号较高」包不包括「中」 → **不包括**（含中最高 67.0%，不含中 88.4%，4/4 批一致）**不用再拍**
  - 品牌词（46 行）落哪一档 → 落低 88.4% 好于落中 83.2%，但残差有 63 行反向 ⇒ **要拍**
  - 大词是否强制落低 → 87.9% vs 88.4%，**在噪声内，历史没定**
- 两种读法在本周 300 行上的产物：**高 40 行**（历史读法）vs **高 112 行**（把中算较高）⇒ 读法不钉住的实际代价。
- 另有前置发现：`细分标签` 命名规范**在批次之间变过**（08-26 带前缀 → 08-29~09-12 裸词 → 09-19 又带前缀），
  `用户意图` 也整体翻过（购买型占优 → 了解型占优 → 又回购买型）⇒ **规则段的产出本身在批次间不稳**，
  这会影响任何跨批次的标签统计。

## 这份证据证不了什么

- 它只证明**现状**（哪些字段空、缺哪个选项、哪个实现存在），**不证明**本地 AI 段能跑通到落库 —— 那段还没写。
- `细分标签` 差集是按**本周这 300 个关键词**算的；下周换一批关键词可能又出现新选项缺口
  （现有选项里还留着 `尺寸/50cm`、`尺寸/60cm`、`尺寸/70cm` 三个**引擎永远不会产出**的 `cm` 形式孤儿，
  说明历史上有人手工加过，而引擎的约定是 `厘米`；本轮只补了引擎实际需要的那一个 `尺寸/70厘米`）。
- 上周 300/300 已填**说明有人/有流程填过**；这个「谁填的」在 10 号文件里给到了能证的部分
  （老 AI 段代码 + 08-29 的修复回执），但**08-26 / 09-11 / 09-12 三批的具体路径仍无记录，不猜**。
- `10-content-heat-rule-trace.txt` 里有一处**对自己上一轮说法的订正**（`AI预测-` 前缀问题）。
  留在这里是为了下次不要再照着旧说法推。
