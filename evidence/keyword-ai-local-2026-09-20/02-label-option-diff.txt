只读探针：D:\Retire\probe-live\diff-label-options.mjs
它 import 仓库内的 runtime/local-keyword-analysis.mjs，所以「引擎会产出什么」与生产是**同一份源码**。

$ node diff-label-options.mjs tblZsUns9353w3nl
细分标签：type=4 现有选项 223 个
（现有选项全表略 —— 见 INDEX.md 引用的完整清单；此处只留与差异有关的部分）
  含：尺寸/1米 尺寸/1.2米 尺寸/1.3米 尺寸/1.4米 尺寸/1.5米 尺寸/2米
      尺寸/50cm 尺寸/60cm 尺寸/70cm 尺寸/60厘米宽
  不含：尺寸/70厘米

规则引擎共产生 114 种标签，其中 1 种不在字段选项里：
  尺寸/70厘米  ← 70cm小浴缸

关键词分类 选项 9：场景词 / 大词 / 尺寸词 / 材质词 / 痛点词 / 款式词 / 风格词 / 功能词 / 品牌词
  本次产生的取值：大词=22, 场景词=62, 品牌词=46, 尺寸词=34, 材质词=32, 功能词=36, 风格词=16, 款式词=52
  全部已有选项

用户意图 选项 26（其中 21 个是「<关键词> | <意图>」形式的脏选项，真正常用的 5 个都在）：
  了解型 / 对比型 / 购买型 / 灵感型 / 问题解决型
  本次产生的取值：了解型=15, 购买型=250, 灵感型=28, 对比型=5, 问题解决型=2
  全部已有选项

共 300 行；有标签的行 286

---- 我的解读（不是探针输出） ----
1. 三个字段里只有 细分标签 有缺口，且**正好缺口 1 个选项**，由**正好 1 个关键词**（`70cm小浴缸`）触发。
2. 缺口的形态值得记一笔：字段里已经有 `尺寸/70cm`，而引擎把 `cm` 归一成 `厘米`（`local-keyword-analysis.mjs:262`
   `.replace(/cm$/iu,'厘米')`）⇒ 引擎要的是 `尺寸/70厘米`。
   现存选项里 `50cm/60cm/70cm` 三个是**引擎永远不会产出的孤儿**（历史上手工加的），
   而 `尺寸/60厘米宽` 才是引擎的形态 ⇒ 库里两套命名并存，这才是这次缺口的根。
3. `用户意图` 选项列表里那 21 个「<关键词> | <意图>」是历史脏数据（不是本轮的错），本轮不处理。
