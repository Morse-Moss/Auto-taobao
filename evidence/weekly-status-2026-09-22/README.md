# 周报全流程状态（2026-09-22 09:44 实测，全程只读）

问题：周报的全流程跑通了吗。

答案：**没有。** 关键词段基本走完（还差灰豚段 + 两列长期空），竞品段卡在最后两步，且两段之间没有编排入口。

口径：周报 = 两段，先关键词收集、后竞品收集（`docs/ops/WEEKLY-FLOW-CURRENT-2026-09-20.md` 开头自述）。
周口径 = 周日~周六 ⇒ 上一期 = 2026-09-13~2026-09-19；本期 2026-09-20~2026-09-26 进行中（今天周二），**上述所有「缺」都不含本期**。

## 取证方式

四个既有只读探针各重跑一次（全部只发 GET；`records/search` 是读语义），产物在同目录：

| 产物 | 探针 | 回答什么 |
| --- | --- | --- |
| `keyword-week-table-2026-09-19.txt` | `D:/Retire/probe-live/keyword-week-state.mjs` | 关键词周表逐列有值率 |
| `keyword-history-batches.txt` | `probe-live/probe-keyword-batches.mjs` | 关键词历史总表批次与有效性 |
| `competitor-latest-week-and-main.txt` | `probe-live/competitor-weekly-state.mjs` | 竞品最新周表 / 主表 / SKU明细 关键列 |
| `competitor-weeks-across-periods.txt` | `probe-live/weekly-tables-across-periods.mjs` | 四期竞品周表并排 |
| `both-history-batches.txt` | `probe-live/history-batches.mjs` | 两张历史总表的周期与有效性 |

## 一、关键词段（上一期 09-19）

周表 `关键词分析 V1（2026-09-19）` `tblZsUns9353w3nl`：29 字段 / **300 行**。

| 步骤 | 状态 | 判据 |
| --- | --- | --- |
| 1.1 导出七天搜索排行 | 通 | 排名 1-300 连续唯一（列 300/300） |
| 1.2 本地落盘 | 通 | 本轮未单独取数（无外部副作用，不构成阻塞） |
| 1.3 PUBLISH#1 真写飞书 | 通 | 周表 300 行；历史总表批次 8 = 300 行「有效」 |
| 1.4 内容热度 | 通 | `内容热度` 300/300（中200 低60 高40） |
| 1.5 灰豚 | **没跑** | `灰豚话题浏览量` **1/300**（唯一那个值是 09-21 探针留下的 327000） |
| 1.6 决策历史同步 | 通 | 三个 `上一有效周…达标` **300/300**；三个 `近2周…达标次数` **300/300**；`是否重点词` 否(296) 是(4) |
| — | **长期空列** | `已有有效批次数` **0/300** |

- `细分标签` 290/300（96.7%）是 MultiSelect 的正常形态，不是缺口。
- 关键词历史总表 `tbl7HbH11JsQx6FL`：**2367 行**；批次 1-8，其中批次 2（267 行）标 `无效-周期错误`（更早的 08-14 期）⇒ **有效 2100 行**。
- 1.6 这一步在 2026-09-21 之前是「靠人记得跑」，漏过两次（08-26 批次 4、09-19 批次 8）；09-21 起被接进 `runtime/run-keyword-weekly-local-analysis.mjs` 第三段 —— 本次实测三列与 `是否重点词` 均有值，说明那次补跑与接线确实落地。

## 二、竞品段（上一期 09-13~09-19）

最新周表 `竞品周_2026-09-13_2026-09-19` `tbllWI45sK0DfHpr`：39 字段 / **1417 行**。

| 列 | 本期有值 | 与前三期并排 |
| --- | --- | --- |
| 搜索关键词 | 1417/1417 100% | 08-30、09-06 两期 **0%** |
| 尺寸 / 适用空间 | 1417/1417 100%（**Text**） | 08-30、09-06 **整列缺字段**；08-23 期是 Lookup **0.8%** |
| 数据状态 / 待补数据项 | 1394/1417 98.4% | 08-30、09-06 缺字段 |
| 五列属性（材质/外形/安装/功能/风格） | 1417/1417 100% | 08-23 期 54.1%；08-30、09-06 期 0% |
| 商品ID | 1417/1417 100% | 08-23 期 100%；中间两期 100% |
| 竞品分类（公式） | 1417/1417 100% | 四期都 100% |

⇒ 四期里**唯一写满的一期**。「竞品链跑不通」与实测不符：**采集 → 导入 → 属性写回 → 分类 → 发布这一段本期是通的**。

仍然断的：

1. **第 6 步 SKU 富化 —— 硬阻塞**。全 base 只有 1 张 SKU 周表（`SKU周_2026-08-23_2026-08-29`，734 行），**本期从未创建**。阻塞是买家号登录态（`evidence/sku-step6-2026-09-20/preflight-01.txt`：被重定向到 `login.taobao.com/havanaone/`，只能人扫码）。
2. **第 7 步产物落点不完整**。竞品主表 `尺寸`/`适用空间` 仍是 Lookup(19)、**15/2004 = 0.7%**（平台枷锁：开放 API 建不了/改不了 Lookup）。本期靠「周表改 Text 写满」绕行 —— 数据在，但**流不进主表那两列**。
3. **问题库只有 2 张**（08-23~08-29、09-13~09-19），中间两期缺。FAQ 段有自己的发布器，**本次未取证 ⇒ 不给判断**。

竞品历史总表 `tbln7qqA6XopiL4Q`：**5763 行** / 4 个周期（08-23、08-30、09-06、09-13 起，1423/1417/1461/1462 行），`批次有效性` **全标「有效」**。`批次ID`、`来源时间` 两列全空（用途未查）。

## 三、结构性缺口（与前两节并行存在）

1. **两段都没有单一编排入口**。竞品八步靠人按文档顺序逐个跑 `runtime/*.mjs`；`runtime/round-schedule.json` 里 `weekly-competitor` 是 `enabled:false`，且它自述的 capability 写的是**关键词链**的 `sycm.feishu.weekly`。
2. **关键词周更也没进排期**。1.6 虽已接进入口，但属 opt-in（不给 `--history-table-name` 就只跑前两段），且排期里没有关键词周更这一条。
3. **两段之间没有数据接口**。竞品采集的关键词是 CLI 参数、竞品周表的 `搜索关键词` 由常量写入。「先关键词、后竞品」只在人排的顺序上成立，**代码里不是一条数据链**。

## 四、与 `WEEKLY-FLOW-CURRENT-2026-09-20.md` 的两处差异（该文档已过时）

| 文档写法 | 本次实测 |
| --- | --- |
| §7.1 表格：09-13 期 `商品ID` **0%** | **1417/1417 100%**（2026-09-21 的补写生效） |
| §7.2 主表 `商品ID` 1461/2004 **72.9%** | **2004/2004 100%** |

## 五、边界（没查就不给判断）

- 全过程只读：只发 GET（`records/search` 也是读语义），**未写飞书、未启停任何进程**。
- 未查、因此不判断：灰豚为什么长期没跑（只报了 1/300 这个事实）、`已有有效批次数` 这列的用途、问题库中间两期为什么缺、`批次ID`/`来源时间` 为何全空。
- 「排期/无人值守」不在这份取证范围内：这里回答的是**数据面跑通没跑通**，不是**能不能无人值守**。
