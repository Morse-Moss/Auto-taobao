原始 sha256 = 9376b7854ac49ce02954530a7d7cfa22d574d0218b6619da519b9f5cdbec6690
基线：{"failed":[],"pass":"22","fail":"0"}

M1 把「读不到就再等一轮」改回「读不到就抛」—— 复原 09-20 之前的行为
  期望红：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止 / 落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」
  实际红(2/2 fail, 20 pass)：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止 / 落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」
  是否点名到期望的用例：是

M2 不数「读不到几次」—— 报错里就分不开「一次都没读成」和「读成了但对不上」
  期望红：落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」
  实际红(2/2 fail, 20 pass)：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止 / 落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」
  是否点名到期望的用例：是

M3 把预算缩回 2 轮（实测冷挂载 6.2s，两轮远远不够）
  期望红：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止
  实际红(2/2 fail, 20 pass)：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止 / 落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」
  是否点名到期望的用例：是

M4 把读取搬回 try 之外（位置退回旧版）—— 专打那条源码级位置守卫
  期望红：落位：settle 里那次读取必须在容错里（放在 try 之外＝第一次读就穿透）
  实际红(3/3 fail, 19 pass)：落位：navigate 之后头两次读不到（页面还没渲染完）不算失败，等到齐为止 / 落位：一直读不到时，报错要点名「读了几次、最后一次为什么读不到」 / 落位：settle 里那次读取必须在容错里（放在 try 之外＝第一次读就穿透）
  是否点名到期望的用例：是

还原后 sha256 = 9376b7854ac49ce02954530a7d7cfa22d574d0218b6619da519b9f5cdbec6690
逐字节一致：是
还原后基线：{"failed":[],"pass":"22","fail":"0"}
安全网备份（仓库外）：D:/Retire/probe-live/date-picker.mjs.bak
