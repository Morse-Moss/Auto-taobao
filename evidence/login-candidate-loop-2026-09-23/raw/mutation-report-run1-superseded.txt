原始 sha256：io=034b67345e60 core=ddb7b92d1d98
基线（未突变）：pass=53 fail=0

M1 IO：身份守卫判出「填的是别人」却没人照它改道
  结果：pass=52 fail=1 红的是：主脚本真的接了候选循环与身份守卫（判据齐全但没人用＝最难发现的空转）
  期望点到：/主脚本真的接了候选循环与身份守卫/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M2 IO：候选都没成时，不把页签导回承诺的那一页
  结果：pass=52 fail=1 红的是：主脚本真的接了候选循环与身份守卫（判据齐全但没人用＝最难发现的空转）
  期望点到：/主脚本真的接了候选循环与身份守卫/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M2b IO：不受 core 的判定管，一律导页面（连不该导的那两条也导）
  结果：pass=52 fail=1 红的是：主脚本真的接了候选循环与身份守卫（判据齐全但没人用＝最难发现的空转）
  期望点到：/主脚本真的接了候选循环与身份守卫/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M2c IO：导的不是判定选中的那一页（回到「循环结束时碰巧停的那页」）
  结果：pass=52 fail=1 红的是：主脚本真的接了候选循环与身份守卫（判据齐全但没人用＝最难发现的空转）
  期望点到：/主脚本真的接了候选循环与身份守卫/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M3 IO：主脚本又拿单条固定地址当流程
  结果：pass=52 fail=1 红的是：主脚本真的接了候选循环与身份守卫（判据齐全但没人用＝最难发现的空转）
  期望点到：/主脚本真的接了候选循环与身份守卫/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M4 core：优先级表把 NO_FORM 排到最前（人会被引到最贵的那个动作上）
  结果：pass=52 fail=1 红的是：finalLoginVerdict：五选一的优先级，以及「该把人送到哪一页」
  期望点到：/finalLoginVerdict：五选一的优先级/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M5 core：身份守卫一律放行
  结果：pass=52 fail=1 红的是：身份守卫：填进来的账号必须与这家店登记的会员名逐字相同（四态各有明确出口）
  期望点到：/身份守卫：填进来的账号必须与这家店登记的会员名逐字相同/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M6 core：isTaobaoLoginUrl 退回子串判据（旧写法，靠巧合才对）
  结果：pass=52 fail=1 红的是：isTaobaoLoginUrl：按 URL 结构判，不再靠「子串巧合」
  期望点到：/isTaobaoLoginUrl：按 URL 结构判/u ⇒ 命中
  还原逐字节一致：是
  判定：OK

M7 core：候选表只剩一条（就回到「必然有机器填不上」）
  [跳过]

── 全部还原后的 sha256 ──
io: 逐字节一致 (034b67345e60)
core: 逐字节一致 (ddb7b92d1d98)

总结论：所有突变都被点到名的用例抓住，且全部还原。
