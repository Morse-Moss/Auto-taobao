# 09-19 13:12 复盘点：五台实例都在，第五家仍未登录

（本文件是 `00-README.md` 之后的一次**只读复核**，不改结论、只补现场时间戳。用户规则：动手前先盘点，
不信任上一轮的结论。本轮盘点还顺带纠正了一次**探针自身的假报错**。）

## 1 十条监听记录（`netstat -ano` 实测）

| 店铺 | 浏览器 | 代理 | 监听 |
| --- | --- | --- | --- |
| 里可林淘宝 | 19031 | 19041 | LISTENING |
| 网林天猫 | 19032 | 19042 | LISTENING |
| 盖文淘宝 | 19033 | 19043 | LISTENING |
| 科塔淘宝 | 19034 | 19044 | LISTENING |
| **盖文天猫** | **19035** | **19045** | LISTENING |

五个代理 `/health` 逐台回读：`status:ok`、`connected:true`，`browser.id` 各是各的店
（`edge-shop-likelin-home` / `wanglin-flagship` / `suixin-custom` / `j873522735` / `gaiwen-flagship`），
`managedTabs` 2~3、**`pinnedTabs` 恒为 1**（店名标签页没被 15 分钟空闲回收）。

## 2 一次假报错（教训）

第一版盘点脚本对 19045 报「没在监听」，`netstat` 复核后确认**是探针自己的错误路径把 error 对象
串成了 `[object Object]`**（`String(err && err.message || err)` 在这条路径上拿到的是对象）。
⇒ **探针报「端口不通 / 连不上」时，先怀疑探针的错误处理，再下结论**；判端口用 `netstat -ano` 复核。

## 3 第五家现场（19035）

页签构成（CDP `/json/list`，8 个目标 ⇒ 4 个 page + 4 个 iframe）：

- `one.alimama.com/index.html#!/login/index` —— **登录页**
- `sycm.taobao.com/custom/login.htm?_target=…/shop/performance/new#/shop` —— **登录页**
- `file:///…/runtime/shop-window-label.html?shop=盖文天猫&port=19035&…` —— 店名标签页（pinned）
- `about:blank`

两个登录页已 `bringToFront`（各回 `{"ok":true}`）⇒ **窗口推到前台等人登**。
结论不变：**这家店的账号还没登，采集还没跑过。**

## 4 profile 卫生（同一时刻复量）

- `Default/Preferences` 的 `sync.passwords` = **null**（密码不同步；`account_info` 里仍有微软账号
  `1074083863@qq.com`，与 `docs/ops/MULTI-SHOP-AND-INTERACTION-DECISION.md` §5.3.1 的 B 组一致，
  **不是问题**，带不带 `--disable-sync` 都一样）。
- `Default/Login Data`（先复制再读，原文件被 Edge 持有会 `database is locked`）：**`logins` 0 条**
  ⇒ 没把个人密码库、也没把别家店的凭据同步进来。

## 5 本轮的判据与提交

除本文件外，本轮只动了长期记忆类文件：`MEMORY.md` 七度瘦身（5,066 → 2,987 字符，注入口上限所致；
只搬家不删内容，原文快照在 `MEMORY-ARCHIVE-2026-09-19.md`），下放的事实进 `PROJECT-NOTES.md` §32。
代码与判据**未改动** ⇒ 上一份 `07-runtime-suite.txt`（644/644）、`08-skills-suite.txt`（151/151）
仍是当前有效基线，**不需要重跑**。
