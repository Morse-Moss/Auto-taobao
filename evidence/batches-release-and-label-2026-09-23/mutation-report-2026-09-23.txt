如期望变红                  把「一律释放」改回「失败不释放」（用户 2026-09-23：每一轮跑完都要释放）
      not ok 12 - 该不该释放：一律释放 —— 链成功、链失败、链根本没跑到，都放
逐字节一致                  把「一律释放」改回「失败不释放」（用户 2026-09-23：每一轮跑完都要释放） —— 还原后校验
如期望变红                  把店铺首屏改回 about:blank（用户：不要空页）
      not ok 4 - 店铺实例的首屏是它自己的标识页（不是 about:blank）—— 空页与标识页一起解决
逐字节一致                  把店铺首屏改回 about:blank（用户：不要空页） —— 还原后校验
如期望变红                  把登录守卫从批次里摘掉（它会退回「实例没起就查」→ 五家全 HTTP 500）
      not ok 17 - 登录守卫**排在每一批的 start 之后**，且 `--shops` 只给本批
逐字节一致                  把登录守卫从批次里摘掉（它会退回「实例没起就查」→ 五家全 HTTP 500） —— 还原后校验
如期望变红                  把逐批产物名改成一个共用名字（后一批会盖掉前一批）
      not ok 18 - 登录结论的文件名逐批不同（共用一个名字时，后一批会盖掉前一批）
      not ok 19 - 结论交给**每一批**的链，且每批拿到的是各自独立的参数数组
逐字节一致                  把逐批产物名改成一个共用名字（后一批会盖掉前一批） —— 还原后校验
如期望变红                  把「默认不启用分批」改坏（默认每批 2 家）
      not ok 1 - 三步的顺序是「先保证实例在 → 再看一眼登录态 → 再跑链」
      not ok 4 - 日期只有一种给法：默认 yesterday，且从不写死具体日期
      not ok 5 - 定时那一档永远不加 --allow-missing-peer（默认关，要开必须显式）
逐字节一致                  把「默认不启用分批」改坏（默认每批 2 家） —— 还原后校验
如期望变红                  把「起与停同一组目标」改坏（停只停第一家）
      not ok 7 - 起与停作用在同一组目标上（同一个变量渲染，不给「起 3 家停 2 家」留缝）
逐字节一致                  把「起与停同一组目标」改坏（停只停第一家） —— 还原后校验
如期望变红                  把版本号三处一致改坏（package.json 漂一格）
      not ok 2 - package.json 的 version 与 VERSION 逐字一致
逐字节一致                  把版本号三处一致改坏（package.json 漂一格） —— 还原后校验
如期望变红                  把「分批也要 --commit」改坏（静默降级成排练）
      not ok 13 - 启用分批：它**替换**链那一步（不是并排），参数只带分批驱动认得的那几个
      not ok 14 - 启用分批时 `--commit` 必须显式传下去（漏了它就变成「排练」，而日志看不出异常）
      not ok 15 - 启用分批：告警出口与降级开关照旧按需转发
逐字节一致                  把「分批也要 --commit」改坏（静默降级成排练） —— 还原后校验
如期望变红                  把「分批宿主逐批读本批结论」改坏（三批读同一份）
      not ok 24 - 宿主（分批链）也接上了：每一批的链各读**本批**那份结论，且守卫排在本批 start 之后
逐字节一致                  把「分批宿主逐批读本批结论」改坏（三批读同一份） —— 还原后校验

明细：evidence\batches-release-and-label-2026-09-23\mutation-report-2026-09-23.json
