# 上周（2026-09-13~2026-09-19）的收集到底完了没有

用户问：「一般是周末或者周一收集上周的，上周的收集完了吗？」

本项目的周口径是**周日~周六**，所以「上周」＝ `2026-09-13~2026-09-19`。

边界：本轮全部探针**只发 GET**（listTables / listFields / listRecords）＋ 只读本地文件。
未创建、未更新、未删除任何记录或字段，**未启动或停止任何进程**。测量时间 2026-09-21 12:12~12:16。

## 一句话结论

**数据都收进来了，但整条周更没走完。**

- 竞品段：周表 1417 行、历史总表 1417 行（标「有效」）都在 —— 但**采集时间是 09-15，在周期中途**。
- 关键词段：导出与导入完成（300 行 + 历史批次 8）—— 但**AI 结算、决策历史同步两步没做**（与上一期对照确证）。

## 文件

| 文件 | 是什么 |
| --- | --- |
| `period-inventory.txt` | 两张 base 的**全部表清单 + 周期表行数**（只读列举） |
| `history-batches.txt` | 两张历史总表的批次/周期分布（含 `批次有效性`） |
| `ts-convert.txt` | 上面毫秒时间戳 → 可读日期的换算 |
| `keyword-week-state-2026-09-19.txt` | 上周关键词周表**逐列有值率** |
| `keyword-week-state-2026-09-12.txt` | **上一期对照**（判断「这期缺」还是「一直缺」的关键） |
| `competitor-week-dates.txt` | 竞品周表的 `数据开始日期/数据结束日期/采集时间` 取值分布 |
| `weekly-collection-receipt-copy.txt` | `runtime/.weekly-collection-receipt.json` 副本（`at=2026-09-15T10:49Z`） |

## 二、竞品段

| 观测 | 值 |
| --- | --- |
| `竞品周_2026-09-13_2026-09-19` | 1417 行 / 39 字段 |
| 该表 `数据开始日期` / `数据结束日期` | 1417 行全是 `09-13` / `09-19` |
| 该表 `采集时间` | **1417 行全是 `2026-09-15`** |
| `竞品历史总表 V1` 里这期 | **1417 行**，`批次有效性 = 有效` |
| 采集收据 `runtime/.weekly-collection-receipt.json` | `at = 2026-09-15T10:49:58Z`（北京 09-15 18:49），5 个分段（21-24 / 25-28 / 29-32 / 33-36 / 37-40）全 ok、`failures[]` 为空、合并 ok |
| 上一期（09-06~09-12）的 `采集时间` | `2026-09-13`，即**周期结束后第二天** |

⇒ **节奏不一致**：上一期是「周期结束后采」，这一期是在**周期第 3 天（09-15）**采的。
数据本身齐全（五列属性 100%、A/B 尺寸已补齐），但它是一次**中途快照**，不是周末/周一那次。
（`runtime/backfill-weekly-date-fields.mjs:49` 自己就写着：采集时间取实际采集日，出处是那份收据。）

## 三、关键词段

上周表 `关键词分析 V1（2026-09-19）`（300 行 / 29 字段）与**上一期 `（2026-09-12）`** 对照：

| 列 | 上周 09-19 | 上一期 09-12 | 判读 |
| --- | --- | --- | --- |
| 排名 / 搜索词 / 搜索人气 / 点击率 / 支付转化率 / 关键词编号 / 关键词分类 | 100% | 100% | 采集与导入到位 |
| `采集日期` | 300/300 ＝ `2026-09-19` | 300/300 ＝ `2026-09-12` | 期次标注正确 |
| **`内容热度`**（AI 字段） | **0/300 全空** | **300/300**（中/低/高） | **上周的 AI 结算没做** |
| **`上一有效周重点达标` / `A级达标` / `探索达标`** | **三列全空** | **三列 300/300** | **决策历史同步没跑** |
| `是否重点词` | 否 295 / **待数据 5** | 否 294 / 是 6 | 有 5 行未被结算 |
| `灰豚话题浏览量` | 0/300 | **0/300** | **长期未跑**，不是上周才缺 |
| `已有有效批次数` | 0/300 | 0/300 | 同上，两期都空 |

导出段的源证明（`runtime/weekly-runs/2026-09-19/pre-ai-20260919T233400Z/pre-ai-manifest.json`）：
`period=7天`、`2026-09-13 ~ 2026-09-19`、`dayCount=7`、`rowCount=300`、`rankRange=1-300`、
`pageSizes=50/50/50/50/50/50`，状态 `LOCAL_INPUT_READY`。

`关键词历史总表 V1`：2367 行，`批次编号 8 = 300 行`，采集日期 `2026-09-19`，标「有效」。
（另有一批 `批次 2 × 267` 标 `无效-周期错误`，属 08-14，与上周无关。）

## 四、没有下结论的地方（如实标注）

- `问题库_2026-09-13_2026-09-19` 只有 **50 行**，而 `问题库_2026-08-23_2026-08-29` 有 **2023 行**，
  差得很远。**我没有查这条链**（FAQ 有自己的发布器与汇总口径），所以不判断 50 行是正常的新口径还是缺数据。
- 竞品段 `SKU周_2026-09-13_2026-09-19` **不存在**（全 base 只有 `SKU周_2026-08-23_2026-08-29`）。
  `runtime/sync-weekly-sku-history.mjs:164` 找不到该表会 fail-closed ⇒ 这段对上周**没有跑**。
- 本表所有「非空率」都不区分「真值」与「兜底值」：竞品周表 `尺寸` 的 1417/1417 里，
  1364 行是「无注明」（非 A/B 行的正常兜底）。**有值率不等于正确率。**
