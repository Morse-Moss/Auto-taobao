js 原始 sha256(前12) = 49ac168729ec，长度 18139
html 原始 sha256(前12) = 20fd268b9c80，长度 1755

基线：# pass 29 / # fail 0（EXIT=0）

M1 被抓 ✔  [js] --prune 不再独占意图（label 恒为 true）⇒ 清页签会顺带挂标签页，两步计划无法分开下达
   期望点名「--prune 是独占意图」⇒ 实际红：21:--prune 是独占意图：带它就只清页签，不会顺手把标签页也挂了

M2 被抓 ✔  [js] 不再标注「停在登录页」⇒ 提示整条消失
   期望点名「停在登录页的后台页面仍然是 work / 这家店没登录这件事必须落在人看的那一行上 / 没证据时不许说「已登录」」⇒ 实际红：23:停在登录页的后台页面仍然是 work —— 改成 loginPage 会让窗口少一个后台页面 | 24:这家店没登录这件事必须落在人看的那一行上，而不是只挂在对象字段里 | 25:没证据时不许说「已登录」：URL 不是登录页 ≠ 会话有效

M3 被抓 ✔  [js] 把停在登录页的工作页改成 loginPage ⇒ 分组键退化、两个后台页面被合成一组只剩一个
   期望点名「停在登录页的后台页面仍然是 work」⇒ 实际红：23:停在登录页的后台页面仍然是 work —— 改成 loginPage 会让窗口少一个后台页面 | 24:这家店没登录这件事必须落在人看的那一行上，而不是只挂在对象字段里 | 25:没证据时不许说「已登录」：URL 不是登录页 ≠ 会话有效 | 29:「是否停在登录页」只影响提示，不影响清理处置（改提示不该有副作用）

M4 被抓 ✔  [js] 登录页识别恒为假 ⇒ 真登录页也认不出来
   期望点名「登录页识别：」⇒ 实际红：22:登录页识别：对着实测的两种登录页 URL 为真，正常工作页为假 | 23:停在登录页的后台页面仍然是 work —— 改成 loginPage 会让窗口少一个后台页面 | 24:这家店没登录这件事必须落在人看的那一行上，而不是只挂在对象字段里 | 25:没证据时不许说「已登录」：URL 不是登录页 ≠ 会话有效

M5 被抓 ✔  [js] 没有证据时说「已登录」（把「尚未触发」说成「不需要」，本项目犯过两次的错）
   期望点名「没证据时不许说「已登录」」⇒ 实际红：25:没证据时不许说「已登录」：URL 不是登录页 ≠ 会话有效

M6 被抓 ✔  [js] 提示只挂在字段里、不落到人看的那一行上
   期望点名「这家店没登录这件事必须落在人看的那一行上」⇒ 实际红：24:这家店没登录这件事必须落在人看的那一行上，而不是只挂在对象字段里

M7 被抓 ✔  [js] 有登录页证据却报「已登录」⇒ 收信人被告知万事大吉
   期望点名「没证据时不许说「已登录」」⇒ 实际红：25:没证据时不许说「已登录」：URL 不是登录页 ≠ 会话有效

M8 被抓 ✔  [js] 去掉 browserPage 分类 ⇒ 浏览器引导页又变成「其它页面」（等于没说）
   期望点名「浏览器自带的页面单列一类」⇒ 实际红：26:浏览器自带的页面单列一类（不塞进「其它页面」，那等于没说），且不进关闭计划

M9 被抓 ✔  [js] 关完不回读：把「尝试关」直接当成「关掉了」⇒ /close 的假成功会被写成成功
   期望点名「关完必须回读」⇒ 实际红：27:关完必须回读：/close 报成功但页面还在 ⇒ 进 failed，不许当成功

M10 被抓 ✔  [js] browserPage 不再标 unclosable（改回「保留第一个」）⇒ 关不掉的页面又回到关闭计划
   期望点名「浏览器自带的页面单列一类」⇒ 实际红：26:浏览器自带的页面单列一类（不塞进「其它页面」，那等于没说），且不进关闭计划

M11 被抓 ✔  [js] prunePlan 忽略 unclosable ⇒ 关不掉的照样进关闭计划（每轮一个假动作）
   期望点名「浏览器自带的页面单列一类」⇒ 实际红：26:浏览器自带的页面单列一类（不塞进「其它页面」，那等于没说），且不进关闭计划

M12 被抓 ✔  [js] 删掉 browserPage 的策略 ⇒ 会静默按 other 处理（新增一类却忘了写策略的那种坑）
   期望点名「清理计划的安全阀 / 浏览器自带的页面单列一类」⇒ 实际红：17:清理计划的安全阀：任何「必须保留一个」的类只要存在，就至少留一个（永不把某类清空） | 26:浏览器自带的页面单列一类（不塞进「其它页面」，那等于没说），且不进关闭计划

M13 被抓 ✔  [html] 页面把登录状态的参数名改掉 ⇒ 脚本写的 state 页面读不到，状态行永远不显示
   期望点名「标签页 URL 的参数名必须与页面读的参数名一一对得上」⇒ 实际红：12:标签页 URL 的参数名必须与页面读的参数名一一对得上

M14 被抓 ✔  [html] 状态行改成无条件显示 ⇒ 没量到的窗口上多出一行空白（又回到「看不出来」）
   期望点名「登录状态必须真的落到页面元素上」⇒ 实际红：13:登录状态必须真的落到页面元素上，且是「量到才显示」

还原后 js sha256(前12) = 49ac168729ec（原 49ac168729ec）⇒ 逐字节一致 ✔

还原后 html sha256(前12) = 20fd268b9c80（原 20fd268b9c80）⇒ 逐字节一致 ✔

突变总数 14，被抓 14／14
  M1  CAUGHT  fail=1  点名：21:--prune 是独占意图：带它就只清页签，不
  M2  CAUGHT  fail=3  点名：23:停在登录页的后台页面仍然是 work —— 改 / 24:这家店没登录这件事必须落在人看的那一行上，而不 / 25:没证据时不许说「已登录」：URL 不是登录页 
  M3  CAUGHT  fail=4  点名：23:停在登录页的后台页面仍然是 work —— 改
  M4  CAUGHT  fail=4  点名：22:登录页识别：对着实测的两种登录页 URL 为真
  M5  CAUGHT  fail=1  点名：25:没证据时不许说「已登录」：URL 不是登录页 
  M6  CAUGHT  fail=1  点名：24:这家店没登录这件事必须落在人看的那一行上，而不
  M7  CAUGHT  fail=1  点名：25:没证据时不许说「已登录」：URL 不是登录页 
  M8  CAUGHT  fail=1  点名：26:浏览器自带的页面单列一类（不塞进「其它页面」，
  M9  CAUGHT  fail=1  点名：27:关完必须回读：/close 报成功但页面还在 
  M10  CAUGHT  fail=1  点名：26:浏览器自带的页面单列一类（不塞进「其它页面」，
  M11  CAUGHT  fail=1  点名：26:浏览器自带的页面单列一类（不塞进「其它页面」，
  M12  CAUGHT  fail=2  点名：17:清理计划的安全阀：任何「必须保留一个」的类只要 / 26:浏览器自带的页面单列一类（不塞进「其它页面」，
  M13  CAUGHT  fail=1  点名：12:标签页 URL 的参数名必须与页面读的参数名一
  M14  CAUGHT  fail=1  点名：13:登录状态必须真的落到页面元素上，且是「量到才显
