CAUGHT  刷新：去掉 yesterday 的格式校验（null===null 会把「读不到」判成「已是昨日」）
          应红的是「刷新判定：缺了 yesterday 直接抛」；实际红的是：刷新判定：缺了 yesterday 直接抛 —— 它是「读不到」与「已是昨日」之间唯一的分界
        （已还原，sha256 9806b56a4b02，13642 字节，逐字节一致）

CAUGHT  刷新：把「读数读不到」也算 ready
          应红的是「刷新判定：读数是昨日才算 ready」；实际红的是：刷新判定：读数是昨日才算 ready，读不到一律不算（不许把「不知道」判成「已是昨日」）
        （已还原，sha256 9806b56a4b02，13642 字节，逐字节一致）

CAUGHT  刷新：汇总时忘掉「还有一家要刷新」
          应红的是「刷新汇总：在位性沿用 shop-pages 的判据」；实际红的是：刷新汇总：在位性沿用 shop-pages 的判据（缺口 / 需人决定 / 连不上都算不 ok）
        （已还原，sha256 9806b56a4b02，13642 字节，逐字节一致）

CAUGHT  刷新：重载改用 /navigate（同 URL 导航＝同文档导航，浏览器什么都不做）
          应红的是「重载：走的是页面自己 reload」；实际红的是：重载：走的是页面自己 reload，不是 navigate（同 URL 的 navigate 是同文档导航，什么都不做）
        （已还原，sha256 9806b56a4b02，13642 字节，逐字节一致）

ERROR   驱动：失败路径的收尾整个不接
          写入没生效（静默丢写）
        （已还原，sha256 fadabff1a478，68351 字节，逐字节一致）

ERROR   驱动：收尾被挪到「停整轮」之后（默认策略下唯一失败的那家不会被收尾）
          写入没生效（静默丢写）
        （已还原，sha256 fadabff1a478，68351 字节，逐字节一致）

突变验证：4/6 被抓住
exit=0
