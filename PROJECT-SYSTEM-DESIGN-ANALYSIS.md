# 系统设计第一性原理分析

日期：2026-09-11
性质：设计分析（决策支持文档，尚未经批准成为架构决策）
读者：项目所有者
关联：docs/architecture/README.md、docs/standards/README.md、agent-runtime/CLAUDE.md、PROJECT-HANDOVER-ANALYSIS.md、PROJECT-TECH-DEBT-GOVERNANCE-PLAN.md

## 1. 现有设计资产盘点

本项目不是没有架构设计，而是有一套相当完整的设计体系，分为四层：

1. docs/architecture/README.md：目标架构基线。定义系统定位（任务执行平台，非 Agent 聊天系统）、非目标、16 类核心资源（Tenant/Store/Run/Step/Lease/EvidenceManifest 等）、五层分层（控制平面 / Durable Workflow / Browser Broker / Adapter-Worker / Agent Runtime）、权威数据职责表、统一证据链、统一状态与失败模型、并发分 lane 规则、四大业务流程落位、迁移路线、11 条架构验收。
2. docs/standards/README.md：工程治理基线。文档权威矩阵、目录所有权、模块边界、状态证据不变量、并发隔离、测试验收门禁、可复现性缺口清单、安全与数据治理、变更治理、Agent 权限边界、Git 规范、落地顺序。
3. agent-runtime/CLAUDE.md + temporal/：Temporal POC 原型及其 5 条真实使用验收标准（进程被杀恢复、commit-before-response 幂等、人工闸门存活、受限 Agent 调用、真实 PostgreSQL 游标集成）。当前全部未达标，浏览器/提交/租约 activity 均为假适配器。
4. 代码中沉淀的事实架构：9 个 Skill、PostgreSQL 权威状态合同（XWS adaptive）、advisory lock 所有权、证据收据体系（operator-status.json、candidate receipt、publication receipt）、确定性验证器（validate-output.py）、人工闸门硬停止（HUMAN_REQUIRED）。

结论：设计不是缺失，而是"目标设计相当完整、代码实现是其中底部三成"。真正的问题是：这套目标设计是正确的吗？哪些部分是第一性原理推导出来的必然，哪些是想象出来的负担？这决定接下来多 agent 改造该往哪使劲。

## 2. 第一性原理：回到公理

抛开所有框架名词（Temporal、LangGraph、多租户、控制平面），回到这个系统赖以存在的物理事实。以下 8 条公理每一条都有本仓库的直接证据支撑。

公理 1：外部世界不可控且会变。
平台页面随时改版、登录态会过期、风控会随机介入、插件会停滞。证据：XWS 真实 run 在 20/40 页 STALLED；browser.id 必须精确等于 edge 否则硬失败；SKILL.md 里到处是 HUMAN_REQUIRED 停止条款。
推论：平台交互的脆弱性必须被隔离在一个边界内（Adapter 层），页面变化不允许传播到工作流和业务状态。

公理 2：观察是唯一的事实来源，而单次观察不可信。
文件名会说谎（价格从高到低 vs 实际销量排序）、页码选择器显示 50 行而表格还在渲染旧行数据、下载的文件可能属于上一次意图。证据：export-market-analysis SKILL 明文规定"文件名可以是过期或误导的"。
推论：任何单个观察不能推进业务状态。必须存在独立于观察者的确定性验证层，形成 Observation -> Candidate -> Validated 的证据链。

公理 3：外部写入是副作用，而崩溃后重试不可避免。
飞书写入、表替换都是外部副作用。进程可能在"已写入但未收到响应"之间死掉，重试就会双写。证据：publish-faq-detail-enrichment 的 prepare/switch 两阶段设计、candidate receipt 的 SHA-256 绑定。
推论：所有外部写入必须幂等（幂等键 + 提交账本）、可回滚（改名而非删除、备份）、可验收（回读 + 内容哈希比对）。

公理 4：进程随时会死，只有事先持久化的状态才能幸存。
电源、崩溃、网络断开，都会在任意两条指令之间发生。证据：PostgreSQL Runtime Contract 里"进度只有在对应本地工件存在且范围/验证/大小/SHA-256 均核实后才能推进"。
推论：必须有一个进程之外的持久状态权威（PostgreSQL），本地 JSON 只能是投影。恢复 = 重新读取权威状态 + 从验证过的游标继续。

公理 5：人是系统的组成部分，不是异常。
登录、验证码、额度、授权不是要绕过的错误，而是系统的正常状态。证据：AGENTS.md 明令"Stop for login, CAPTCHA..."；FAQ 流程的人工队列是正式阶段而非降级路径。
推论：人工闸门必须是一等状态（WAITING_HUMAN/APPROVED/DENIED/EXPIRED），且必须能在人工处理后精确恢复，不重跑已验证数据。

公理 6：语言模型是概率性的，确定性工作不能交给它。
LLM 会捏造、会漂移、无法对同一输入保证同一输出。证据：faq-text-analysis 明文"禁止调用飞书 AI 代替固定规则"；AI 复核必须过任务身份/连续原文证据/置信度/一致性质量门。
推论：确定性的部分（状态推进、验证、提交、游标）必须由确定性代码拥有；LLM 只允许出现在语义层（分类复核、自然语言解析、异常分诊），且输出必须是带 schema、带版本、可拒绝的 proposal。

公理 7：物理瓶颈是单数，不是复数。
一个 Edge 实例、一个已登录账号、一个共享 CDP Proxy、一个小旺神额度。加再多的"Agent"也造不出第二个浏览器会话。证据：Browser Broker 设计里"同一账号/profile 默认并发 1"；本项目实际只有一个操作者一个店铺。
推论：本系统的并发模型是"资源分 lane + 单 lane 内串行"，不是"增加 Agent 数量"。真正的并行只存在于三个地方：会话内的商品 fan-out（FAQ 的 5 个商品）、本地 CPU 计算（解析/分析/验证）、不同平台之间（浏览器采集与飞书 API 写入）。"多 agent"的价值不在于并行提速，而在于职责分离与可审计。

公理 8：操作者是非技术人员。
界面必须是自然语言进、状态报告出，内部复杂度不允许泄漏。证据：xws-faq-operator SKILL"运营只说自然语言，Codex 负责解析周期并执行命令""不要要求运营阅读 JSON"。
推论：系统需要一个"翻译层"：把自然语言解析为带参数的命令、把机器收据翻译成人话。这个翻译层目前由 CC/Codex 人肉扮演，没有代码化。

## 3. 从公理推导架构

把 8 条公理当作约束求解，架构几乎是唯一解，逐层推导如下。

第一层（真相层）：由公理 4。
存在进程外的持久权威。当前形态：PostgreSQL（业务事实、验证游标、advisory lock 所有权）+ 不可变本地工件（CSV/XLSX/JSONL 及其哈希记录）。已实现，XWS adaptive 合同是完整范例。

第二层（证据层）：由公理 2 + 公理 3。
确定性验证器 + 证据链 + 幂等提交账本。已实现：validate-output.py、各阶段 receipt、prepare/switch 两阶段发布、SHA-256 绑定。FAQ 流程的收据体系是完整范例。

第三层（执行层）：由公理 1 + 公理 4 + 公理 5。
确定性阶段控制器（状态机）：读权威状态 -> 判定下一阶段 -> 执行一次 -> 状态回写。每个阶段边界都是崩溃安全的恢复点。已实现形态：run-faq-operator.mjs 的 --status/--advance 模式、XWS supervisor（WIP 中）。这一层就是本系统对"Durable Workflow"的正确回答，详见第 4 节。

第四层（适配层）：由公理 1。
按平台封装浏览器交互（导航、配置、导出、诊断），输入输出是合同而非 DOM 细节。已实现形态：9 个 Skill 的 scripts 目录。欠账：Skill 内脚本与 run-* 入口的依赖方向尚未完全理顺（技术债 P2.2）。

第五层（语义层）：由公理 6 + 公理 8。
这是"Agent"唯一合法的居住地，且只有两种角色：
- 入口 Agent：自然语言 -> 周期解析 -> 读状态收据 -> 决定下一阶段命令 -> 执行 -> 把收据翻译成人话。它不拥有状态，每次只推进一个阶段，推完即复读收据。
- 复核 Agent：faq-ai-review 已有实现。带版本 prompt、带质量门、产出结构化结论、失败进人工队列。
移除这两个 Agent，系统仍能以命令行方式完整运行（架构验收第 10 条），这是语义层的设计红线。

人工层：由公理 5。
人工闸门贯穿三、四、五层，是状态不是异常。

## 4. 对现有设计的批判性评估

现有设计总体上是从真实痛点里长出来的（PostgreSQL 合同、证据链、两阶段发布都对应真实事故），方向正确。但从第一性原理审视，有三类问题。

4.1 过度设计（为目标想象付账）
多租户/配额/带权公平队列/Object Storage/控制平面：这些服务于"几十家店铺"的想象规模。公理 7 说当前物理瓶颈是单数。设计的正确姿势是：保留接口缝（Run/Step/Lease 已经是正确的资源抽象），但不建设施。docs 本身也承认（非目标一节），但分层图和资源模型会诱导后来者先建控制平面。应明确：这些是缝，不是路。

4.2 底气不足的地方（真正的缺口）
- Temporal POC 的 5 条验收本质上是正确的验收，但第一性原理追问：本系统当前到底需不需要 Temporal？公理 4 的要求是"状态先持久化再动作"，单进程状态机 + PostgreSQL 事务提交 + advisory lock + 可重启驱动已经满足。Temporal 的增量价值只在：跨机多 Worker、长期 Timer、复杂重试拓扑。当前都不存在。结论：Temporal 是公理 7 改变（多浏览器/多机）之后的事，不是现在的依赖。现有 run-faq-operator 的"一次一阶段 + 状态收据"模式就是当前规模下 Durable Workflow 的最小正确实现。
- 入口 Agent 没有代码化（公理 8 的翻译层完全依赖会话里的 LLM 人肉执行 SKILL.md）。这是最大且最值得做的缺口：一个确定性的调度状态机（读收据 -> 推进一阶段 -> 复读），把"LLM 读 SKILL 凭自觉"变成"机器读收据按状态表行动"。
- Skill 内脚本与 runtime 入口的依赖倒挂（技术债 P2.2）：架构上 runtime 应该是驱动者，现状部分脚本反向依赖，影响第四层的可替换性。

4.3 概念污染
"多 agent"这个词本身需要消毒。本仓库语境下合法的"多 agent"是第五层的两类概率角色 + 三层确定性机器 + 人工闸门的协作（我上一轮提出的五角色方案）。如果把"多 agent"理解为多个 LLM 自主对话、互相委派、共享决策权，那会同时违反公理 6（概率体不拥有确定性状态）和公理 3（概率体不拥有写路径），架构文档和工程规范都明文禁止。GPT6 之前参与讨论的 agent-runtime/CLAUDE.md 里"hardcoded decisions do not count as Agents"一句话说得很准：真 Agent 必须是带受限工具的真实模型调用，而不是把 if-else 叫做 agent。

## 5. 系统设计方案

综合推导结果，当前系统的目标形态如下，全部落在已有代码模式上，不引入新框架。

5.1 结构
- 真相层：PostgreSQL（XWS run 状态、验证游标、advisory lock）+ 不可变工件 + 哈希账本。已有，不动。
- 证据层：验证器 + 收据体系 + 两阶段发布。已有，不动。
- 执行层：每个流程一个确定性阶段控制器。FAQ 已有（run-faq-operator）；XWS 的 supervisor 是用户 WIP，方向正确，补测试即可。
- 适配层：各 Skill scripts。理顺依赖方向（技术债 P2.2），其余不动。
- 语义层：入口 Agent（新开发，约 200-300 行确定性状态机 + 自然语言薄壳，LLM 只做解析和汇报，不做状态决策）+ 复核 Agent（faq-ai-review，已有）。
- 人工层：现有 HUMAN_REQUIRED 硬停止，不动。

5.2 多 agent 的定义（本次改造的验收口径）
调度 = 确定性状态机读 operator-status / run 状态收据，一次推进一个阶段；概率性 LLM 只出现在入口翻译和 FAQ 复核两处，输出必须是可拒绝的结构化结论；采集、验证、提交全部确定性；登录/验证码/风控/授权一律停机给人。移除两个 LLM 角色后系统可用命令行完整运行。

5.3 两条流程的端到端验收
- XWS：新 run 1-40 -> supervisor 推到 DONE -> validate-output 通过 -> xws-to-feishu-base 导入 -> 16 字段、行数、附件数三方一致收据。
- FAQ：新周期 -> 7 阶段全部收据齐 -> REPLACEMENT_APPLIED_AND_VERIFIED -> DONE。

5.4 落地顺序（与此前对齐的 P1-P5 一致）
P1 补完用户 supervisor WIP 及测试 -> P2 开发入口调度状态机（两流程共用）-> P3 真实跑 XWS 端到端 -> P4 新周期跑 FAQ -> P5 修复发现的问题。
其中 P2 是唯一的新架构增量，其余都是把已有推导结果跑实。

## 6. 明确不做的事

- 不引入 Temporal/LangGraph/ADK/AutoGen 作为生产依赖（公理 7 未改变前）。
- 不建控制平面、多租户、配额、Object Storage 的任何实施代码，只保留资源模型中的接口缝。
- 不做多个 LLM 自主协作的 agent swarm（违反公理 3/6）。
- 不绕过任何登录/验证码/风控/额度控制（公理 5，AGENTS.md 最高规则）。
- 不让 Agent 拥有业务状态、写路径、账号选择权或游标推进权。

## 7. 待所有者决策

1. 认可第 5 节方案后，本文档第 4/5 节可提炼为 docs/architecture/ 的正式决策记录。
2. P3/P4 真实运行的时机与小旺神额度授权。
3. FAQ 新周期的日期范围与发布目标表 ID（可到发布阶段再给）。
